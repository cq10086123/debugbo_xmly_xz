# 卡密设备绑定（一卡一机）需求方案【决策已确认版】

> ⚠️ **历史文档（已废弃，2026-08-23）**：本方案描述的「按浏览器设备 ID 绑定 + 席位模型」
> 已被更宽松的「网络绑定（一卡一 IP，同 IP 不限设备）」取代，现行机制见 README「进阶能力」
> 第 5 节与 `core/device_binding.py`。以下内容仅作设计过程存档。

> 状态：**✅ 已实施完成（2026-08-23）**，测试与冒烟验证全通过；上线后到后台「系统配置」开启 `device_binding_enabled` 即生效
> 背景：当前卡密可在任意电脑、任意数量的设备上同时登录使用，无任何限制。
> 目标：实现「插件 + 网页版只在一台电脑使用，别处登录则原设备立刻失效」。

---

## 〇、已确认的四个决策（2026-08-23 与作者确认）

| # | 决策项 | 结论 |
|---|--------|------|
| D1 | 已绑定后新设备登录 | **顶号自动换绑**：新设备登录成功，旧绑定设备被踢并自动失效 |
| D2 | "一台电脑"的口径 | **双席位模型**：每卡 1 个网页席位 + 1 个插件席位，席位内最后登录者独占 |
| D3 | 设备数 | **统一默认 1 台（每席位），管理端可按卡调整为 N**（N=允许共存的最近登录设备数） |
| D4 | 首期范围 | **核心 + 风控加固**：绑定/顶号/插件401处理/后台解绑与设备列表 + 多IP并用检测 + 设备变更日志 + 会话空闲过期 |
| D5 | 实现约束（补充确认） | **模块化、非侵入**：设备绑定全部逻辑放独立新模块；核心功能（搜索、下载、自动重试、账号扫码、文件管理）的业务代码**零改动**；带总开关可一键回退 |

### D5 的架构依据（已核实代码）

所有业务路由（search / download / files / accounts / extension / announcements）的鉴权
**全部收敛在 `api/deps.py` 的 `get_current_card` / `get_current_card_download` 这一个咽喉点**，
下载引擎与自动重试（`api/download.py` 内部、`core/downloader.py`）从不直接读会话表。
→ 因此设备管控只需在 `_auth_card()` 末尾追加一次模块调用，**所有业务端点文件零改动**，
管控效果自然覆盖搜索/下载/重试/插件等全部入口。

### D2 的建模说明（重要）

网页（localStorage）与插件（chrome.storage.local）是**两份隔离的存储**，各自的设备 ID 无法互认"我们在同一台电脑"。因此"一台电脑"只能按席位近似：

```
一张卡密
 ├─ 网页席位：最多 N 个网页设备绑定（N=max_devices，默认 1）
 │    └─ 同席位仅保留最新登录 token（网页互踢）
 └─ 插件席位：最多 N 个插件设备绑定
      └─ 同席位仅保留最新登录 token（插件互踢）
```

- 正常用户（一台电脑，网页+插件一起用）**不受任何影响** ✔
- 已知残余缺口：理论上网页在 A 电脑、插件在 B 电脑各占一席仍可配合使用 —— 由 D4 的
  **"同卡网页/插件出口 IP 不一致"风控信号**兜底（正常用户两者必然同机同出口 IP）。

---

## 一、现状梳理（问题根因）

### 1. 登录与会话机制

| 环节 | 现状 | 代码位置 |
|------|------|----------|
| 登录接口 | 卡密+验证码 → 发放随机 token，**每次登录新建一条 session，旧 session 不失效** | `api/auth.py` `login()` |
| 会话存储 | `sessions` 表只有 `token / card_id / is_active / last_active_at`，**无设备、无 IP、无 TTL 字段** | `db/models.py` `Session` |
| 鉴权校验 | `_auth_card()` 只查 token 有效 + 卡密未禁用/未过期，**无设备/IP/并发数校验** | `api/deps.py` |
| WebUI 持久化 | token 存 `localStorage`，永不过期，401 时清 token 跳登录页 | `frontend/src/utils/request.js` |
| 插件持久化 | token 存 `chrome.storage.local`，**轮询遇到 401 只打日志，不清本地状态** | `extension/background.js` `pollBackend()` |
| 插件轮询 | 每 1 分钟拉一次 `/api/extension/tasks`（天然的"心跳"通道） | `extension/background.js` `chrome.alarms('poll')` |

### 2. 关键业务约束

- 同一台电脑 = 网页 + 插件两个会话（推送任务 → 插件拉取下载是既定工作流），必须共存。
- 卡密列扩展 + 惰性校验的项目先例：`download_mode`、`bound_interfaces`，照此风格扩展。
- 管理后台已有卡密管理页（生成/编辑/续期/删除），是"解绑/设备数"功能的自然入口。
- `card_logs` 表 + `log_card_event()` 已存在，可直接记录设备变更事件。

### 3. 根因总结

**会话只有"有效性"维度，没有"归属"维度** —— token 不绑定任何设备身份，也从不限制
同卡密活跃会话数，一张卡发给多少人就能多少人同时用。

---

## 二、最终行为规格（按已确认决策展开）

### 1. 登录行为（`POST /api/auth/login`，请求体新增 `deviceId`、`client`）

| 场景 | 行为 |
|------|------|
| 卡密未绑定 + 网页登录 | 绑定该设备到网页席位，放行；同席位旧 token（若有）全部失效 |
| 卡密未绑定 + 插件登录 | 绑定到插件席位，放行（两席位独立，互不影响） |
| 该设备已是本席位当前绑定 | 放行（重登录），刷新 token（同端互踢旧 token） |
| 新设备登录、席位未满（max_devices>1） | 追加绑定，放行 |
| 新设备登录、席位已满（默认 1 即满） | **顶号换绑**：踢掉该席位**最早绑定**的设备（其所有会话立即失效），绑定新设备，放行 |
| 卡密禁用/过期 | 维持现状（403/401） |

### 2. 鉴权行为（`api/deps.py` `_auth_card`，请求头新增 `X-Device-Id`）

| 校验 | 失败响应 |
|------|----------|
| token 有效 + 卡密状态 OK（现状） | 401/403（现状） |
| session.device_id == 请求 X-Device-Id | 401 `device_mismatch`（token 被拷到别的浏览器用） |
| session 所属设备仍是该席位当前绑定 | 401 `kicked`（被顶号/被解绑/被风控踢出） |
| 会话空闲未超 TTL（默认 7 天） | 401 `session_expired` |

`get_current_card_download` 的 `?token=` 直链同样走以上校验。

### 3. 客户端行为

| 端 | 改动 |
|----|------|
| WebUI | 首访生成 `deviceId`（UUID，localStorage 独立 key，清 token 不清设备）；登录带 `deviceId+client=web`；axios 全局附 `X-Device-Id` 头；已有 401 拦截清 token 跳登录 ✔；新增 60s `/auth/me` 心跳实现分钟级感知被踢 |
| 插件 | 安装后生成 `deviceId` 存 chrome.storage；popup 登录带 `deviceId+client=extension`；background 请求附 `X-Device-Id`；**`pollBackend()` 遇 401 → 清本地 token + 徽章/弹窗提示「账号已在其他设备登录」**（当前只打日志） |

### 4. 管理后台

- 卡密生成表单 + 编辑弹窗：**绑定设备数**（默认 1）。
- 卡密列表新增列：当前绑定设备（网页席/插件席各显示设备 ID 尾号、绑定时间、最后活跃、最后 IP）。
- 操作按钮：单个设备**解绑**、**全部解绑**、**踢下线**（解绑/踢出即服务端失效，客户端分钟内自行退出）。
- 事件日志：绑定/顶号/解绑/风控踢出全部写 `card_logs`（复用 `log_card_event`）。

### 5. 风控加固（D4）

| 项 | 规则 | 处置 |
|----|------|------|
| 同 token 多 IP 并用 | 同一 token 在短窗口（如 10 分钟）内来自 ≥2 个公网 IP | 立即失效该 token（可配为仅告警） |
| 网页/插件席位 IP 不一致 | 同卡密网页会话与插件会话当前出口 IP 不同 | 告警日志；可配开关：强制踢出较旧一方（默认只告警，观察误伤率） |
| 会话空闲 TTL | 7 天无请求（`last_active_at`，现有字段） | 会话失效，需重新登录 |
| 顶号频率异常 | 短时间大量顶号换绑（设备 ID 频繁变化） | 告警日志，供管理员人工核查 |

---

## 三、数据模型变更（实施时）

```
cards 新增列：
  max_devices   INT  默认 1        -- 每席位允许绑定的设备数

device_bindings 新表：
  id, card_id, client_type(web|extension), device_id,
  bound_at, last_active_at, last_ip
  （card_id + client_type 维度维护最多 max_devices 条，超出按 bound_at 淘汰最早）

sessions 表新增列：
  client_type   VARCHAR(8)         -- web | extension
  device_id     VARCHAR(64)
  ip            VARCHAR(64)        -- 登录时出口 IP
```

迁移策略：`db/init_db.py` 轻量 ALTER TABLE（沿用 download_mode 上线时的做法），
存量卡 `max_devices` 默认 1、**存量已登录会话不踢**（device_id 为空 → 放行），
下次登录起才写入绑定，实现平滑过渡。

---

## 四、边界场景

| 场景 | 行为 |
|------|------|
| 用户清浏览器缓存 / 换浏览器 / 重装系统 | 设备 ID 变化 → 顶号自动换绑（D1），无需找管理员 ✔ |
| 同机网页+插件 | 各占一个席位，互不干扰 ✔ |
| 朋友拿到卡密在自己电脑登录 | 把原设备顶下线；原设备再次登录又会把朋友顶下线 → 双方互踢，无法稳定共享 ✔（这正是 D1 的防共享机理） |
| 手机浏览器用网页 | 占用网页席位，会顶掉电脑网页（按需后续再考虑移动端独立席位） |
| 拷贝 localStorage/chrome.storage 给他人 | 设备 ID 一起被拷走 → `device_mismatch`（token 与设备 ID 一起拷则靠多 IP 并用检测兜底） |
| 管理员解绑 | 原设备分钟内 401 退出；用户重新登录即绑定新设备 |
| 升级部署当天已登录的老会话 | 不踢，下次登录起绑定（平滑过渡） |
| 服务器下载模式 | 不受影响，仅登录态受控 |

---

## 五、安全边界说明（务实预期）

- 纯 Web/插件端不存在不可伪造的硬件标识，任何方案都只能**提高共享成本**：
  本组合下共享者会遭遇持续互踢 + token 拷贝遭 device_mismatch/多 IP 检测失效，
  "一张卡多人日常同时用"被实质阻断。
- 若未来要更强绑定（原生程序机器码 / 浏览器指纹自动认回同机），归入二期，本期不做。

---

## 六、模块化与非侵入设计（D5）

### 1. 新增独立模块（全部逻辑的家，不掺入现有业务文件）

| 新文件 | 职责 | 风格 |
|--------|------|------|
| `core/device_binding.py` | 绑定策略纯函数：席位解析、顶号换绑、绑定有效性、TTL 判定 | 与 `card_helpers.py` 同风格，可纯函数测试 |
| `api/risk_control.py` | 多 IP 并用检测、席位 IP 不一致告警、顶号频率监控 | 独立模块，进程内窗口 + `card_logs` 落库 |
| `frontend/src/utils/deviceId.js` | 设备 ID 生成与持久化（独立 key） | 独立工具文件 |
| `tests/test_device_binding.py` | 绑定/顶号/TTL/device_mismatch 纯函数用例 | 沿用 `tests/test_download_mode.py` 风格 |

### 2. 现有文件改动面地图（可逐项核查）

| 文件 | 改动类型 | 改动内容 |
|------|----------|----------|
| `api/search.py` | **零改动** | — |
| `api/download.py`（下载/重试/断点恢复引擎） | **零改动** | — |
| `core/downloader.py` / `core/batch_runner.py` / `core/account_manager.py` | **零改动** | — |
| `api/files.py` / `api/accounts.py` / `api/announcements.py` / `api/interfaces.py` | **零改动** | — |
| `api/deps.py` | 追加 1 处 | `_auth_card()` 末尾追加一行设备校验调用（开关关闭时直接短路） |
| `api/auth.py` | 追加 | `LoginRequest` 增可选字段；登录成功前调用一次 `device_binding` 模块；验证码/限流/过期判定流程不动 |
| `db/models.py` / `db/init_db.py` | 追加 | 新列（可空）+ 新表，不动现有列与关系（沿用 `download_mode` 上线先例） |
| `api/admin.py` | 追加 | 仅新增设备管理端点，现有管理端点不动 |
| `extension/background.js` | 追加 | 请求头附 `X-Device-Id`；`pollBackend` 的 catch 分支补 401 处理；下载/解析/队列逻辑不动 |
| `extension/popup.js` | 追加 | 登录体附 `deviceId/client`，UI 结构不动 |
| WebUI `request.js` / `auth.js` / `Login.vue` | 追加 | 拦截器加一个头 + 60s 心跳；登录参数透传 |
| `AdminCards.vue` | 追加 | 设备数下拉、设备列表弹窗、解绑按钮 |

> 判定标准：任何核心功能回归问题，只可能来自 `_auth_card` 那一行追加调用；
> 该调用被总开关短路后，行为与当前代码**完全一致**。

### 3. 总开关与平滑回退

- 配置键 `api_config.device_binding_enabled`（沿用 `admin_lan_only` 的开关先例，后台可切、即时生效）
- **关闭时**：登录不要求 deviceId、`_auth_card` 跳过设备校验、`device_id` 为空的存量会话照常放行 → 行为与现在 100% 一致，等价于一键回退
- **开启时**：登录必须携带 deviceId（否则拒绝并提示"请更新网页/插件"）——否则攻击者删掉该字段即可绕过绑定；WebUI 由服务端托管、刷新即更新，插件需要用户更新一次版本
- 存量过渡：老会话 `device_id` 为空 → 放行不踢；下次登录起才写入绑定（D2/R9）

### 4. 回归保护

- 现有 4 个测试文件（`tests/test_download_mode.py` 等）保持全绿：新列全部可空、纯函数测试不依赖新逻辑
- 新增 `tests/test_device_binding.py` 覆盖：席位绑定/顶号换绑/解绑/TTL/device_mismatch/开关关闭短路
- 上线手动回归清单：官方+第三方搜索、服务器批量下载、失败自动重试、断点恢复、插件本地下载（推送→拉取→下载→ack）、扫码登录、文件打包下载、公告推送

### 5. 实施顺序（每步独立可验证，任一步出问题不影响已完成步骤）

1. 数据模型 + 迁移（纯追加，不影响任何现有逻辑）
2. `core/device_binding.py` + 纯函数测试（先测后用）
3. `api/deps.py` 一行接入 + 总开关（默认关）
4. `api/auth.py` 登录接入（开关后才强制 deviceId）
5. WebUI / 插件客户端字段与心跳、插件 401 处理
6. 管理后台设备管理 UI
7. `api/risk_control.py` 风控项
8. 全量回归 → 后台开启开关 → 观察 `card_logs`

---

## 七、实施清单（✅ 已于 2026-08-23 实施完成）

> 改动面以第六节「改动面地图」为准：核心业务文件零改动，其余均为追加式修改。
> 实施顺序按第六节第 5 小节：每步独立可验证，总开关默认关闭，回归通过后再开启。

**后端**
- [x] `db/models.py` / `db/init_db.py`：Card.max_devices、Session 新列、DeviceBinding 新表（追加式迁移）
- [x] `core/device_binding.py`：席位绑定 + 顶号换绑纯函数（先测后用）
- [x] `api/deps.py`：`_auth_card` 追加一行设备校验（受总开关短路保护）
- [x] `api/auth.py`：登录接收 deviceId/client，调用绑定模块（开关开启才强制 deviceId）
- [x] `api_config.device_binding_enabled` 总开关（沿用 admin_lan_only 模式）
- [x] `api/risk_control.py`：多 IP 并用检测、席位 IP 不一致告警、顶号频率监控
- [x] `api/admin.py` + `AdminCards.vue`：设备数配置、设备列表、解绑/踢下线、事件日志展示

**WebUI**（均为追加式：deviceId 工具、登录参数、请求头、60s 心跳、401 文案区分被顶下线）

**插件**（均为追加式：popup 登录参数、background 请求头、pollBackend 401 → 清 token + 提示）

**测试**
- [x] `tests/test_device_binding.py`（纯函数风格，先测后用）
- [x] 现有 4 个测试保持全绿
- [x] 第六节第 4 小节手动回归清单执行一遍（搜索/下载/重试/插件本地下载/扫码/文件/公告）

---

## 八、严格复审记录（2026-08-23，实施后第二轮审查）

### 复审发现并已修复（commit 8d7cff4）

| # | 严重度 | 问题 | 修复 |
|---|--------|------|------|
| 1 | 高（功能坏） | `Guide.vue` 插件包下载直链漏带 `device` 参数，开关开启后必 401 | 直链补 `&device=`，并全库 grep 确认无其他遗漏（Files.vue 两处、Guide.vue 一处全覆盖） |
| 2 | 高（误杀用户） | 开关关闭期登录曾"顺手记录" device_id 但不建绑定；之后开启开关，这批会话因设备不在绑定表被误判「已被顶下线」，违反 R9 平滑过渡 | 关闭期完全不落 device_id（行为与历史版本逐字节一致）；e2e 补该场景回归 |
| 3 | 高（可利用竞态） | 并发登录 TOCTOU：多设备毫秒级并发登录同一卡密可双写超额绑定，绕过席位上限 | `bind_device_on_login` 加进程内互斥锁；实测 8 线程 × 3 设备并发 → 绑定数恒为 1 |
| 4 | 中（误伤面） | 多 IP 风控拿裸 IP 比较：v4/v6 双栈切换、PPPoE 重拨（同 /24）、v6 后缀轮换（同 /48）会误判共享踢人 | IP 网段归一化（v4→/24、v6→/48、私网/回环不作证据）；席位 IP 检测同步归一化 |
| 5 | 低 | 管理员下调 max_devices 后存量超额绑定不收敛，旧设备可用到下次登录 | PATCH 时即时 `trim_bindings_to_limit` 裁剪并失效会话 |
| 6 | 低 | 解绑接口 device_id 无长度校验（进查询与日志） | 限 1~64 字符 |

### 复审确认无问题项（抽查过的关键路径）

- **鉴权咽喉完备性**：全库 grep 确认所有业务路由（search/download/files/accounts/interfaces/extension/announcements）无自建 token 解析，全部经 `deps.get_current_card*`，设备管控覆盖无死角
- **锁与写放大**：绑定维护单锁串行；`log_card_event` 独立连接提交与外层事务无死锁（autoflush=False，外层未持写锁；与 `mark_expired` 同一既有模式）
- **反代理误报**：反向代理场景所有请求同源 IP → 多 IP 检测天然不触发（fail-safe），与 `admin_lan_only` 的直连原则一致
- **级联清理**：删卡级联删 device_bindings（ORM 关系已挂）；过期/续期不影响绑定语义
- **多 worker 部署前提**：进程内锁/风控窗口以单进程为前提（与项目现有验证码限流、任务锁同一前提，Docker 默认单 worker）
- **CORS**：插件携带 X-Device-Id 触发的预检由既有 `allow_headers=["*"]` 覆盖
- **测试数据修正**：v6 测试段 `2001:db8::/32` 被 Python 判为私网（文档段），换真实公网段

### 已知残留限制（设计内，非缺陷）

- 拷贝整个存储目录（deviceId+token 一起）在同网段两台机器使用：由多网段风控兜底，同出口 NAT 内无法区分（纯 Web 方案的天花板，见第五节安全边界）
- 关闭期创建的过渡会话不受设备绑定/空闲 TTL 约束（与历史行为一致），随用户自然重登录消亡；可后台「踢下线全部会话」主动清理

---

## 九、第三轮复审记录（2026-08-23，设备数自定义功能之后）

本轮换打法：不重读已审逻辑，专查前两轮盲区——扩展端存储契约、管理操作与登录的并发交叠、
依赖文件终态通读、fetch/storage 调用点清单化核对、pyflakes 静态检查。

### 发现并已修复

| # | 类型 | 问题 | 修复 |
|---|------|------|------|
| 1 | 高（功能失效） | `popup.js` 的 `getCfg()` key 清单漏了 `auth_error`：`chrome.storage.local.get` 只返回清单内的 key，第 89 行解构 `auth_error` 恒为 undefined → **插件被顶下线后弹窗永远不显示提示**（后台写入的 auth_error 成了死数据） | key 清单补 `auth_error` |
| 2 | 中（并发交叠） | `unbind_device` / `unbind_all` / `kick_all_sessions` 未持 `_BIND_LOCK`，与并发登录的「查席位→淘汰→插入」存在交叠窗口（登录侧按旧快照写入可部分覆盖解绑结果） | 三个管理操作统一持锁（无嵌套获取，无死锁） |
| 3 | 低 | `device_binding.py` 引入的 `Card` 未使用 | 移除（pyflakes 其余告警均为存量代码原有，按最小改动原则不动） |

### 本轮核对通过项

- 扩展端 fetch 清单：6 处发往自家服务器的请求（公告/任务轮询/ack/xm-cookie/cooldown/ping）全部带设备头，无遗漏
- `deps.py` / `device_binding.py` 终态通读：token 失效回查、风控块异常分层（HTTPException 直抛、其余 fail-open+debug 日志）、60s 节流写、锁无嵌套 —— 无新问题
- 管理端数字输入：清空/非数字输入经 `v-model.number` 归一后由前端校验拦截（1~10），后端 400 兜底

### 设计内已知项（接受，非缺陷）

- SSE 流连接在建立时鉴权一次，被顶设备的存量流保持到自然断开（只读任务进度，无写操作）
- 服务器端批量下载任务被踢后继续跑（任务归属卡密，正当所有者重登后仍需进度）；卡密过期有独立任务停止机制
- 浏览器禁用存储的隐私模式下 deviceId 无法持久化——与既有 token 存储同一前提，该场景本就无法登录
