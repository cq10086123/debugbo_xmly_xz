# 有声下载 WebUI — 全项目代码审查报告

> 审查日期：2026-08-16
> 审查范围：后端（app.py、api/ 13 文件、core/ 13 文件、db/ 3 文件）、前端（frontend/src 全部）、浏览器插件（extension/ 全部）
> 方法：四路并行初审 → **高危/中危发现全部由人工逐条对照源码复核**，沙箱逃逸已实际运行复现
> 说明：本报告只列问题，未改动代码。每条附位置与证据，按修复优先级排序。

---

## 一、高危（2 个，建议立即修）

### 🔴 1. Python 脚本接口沙箱可逃逸 → 任意代码执行（已实测复现）

| 项 | 内容 |
|---|---|
| 位置 | `core/script_engine.py:331-369`（`_build_script_globals`）+ `:199-225`（`validate_script`） |
| 根因 | ① exec 的全局字典 `g = dict(_SAFE_BUILTINS)` **没有 `__builtins__` 键** → Python 自动注入**真实 builtins 字典**（含 `__import__`/`open`/`eval`）；② 校验器只查 Import/Call/Attribute 节点，**不拦 `ast.Name` 读取、不拦字符串下标** |
| 复现（已实际运行成功） | `def parse(params):` 内写 `imp = __builtins__["__import__"]; os = imp("os"); return os.getcwd()` —— 校验通过、成功拿到 os 并执行。worker 子进程与后端同权限，等于主机 RCE |
| 触达面 | 仅管理员能创建/调试脚本接口（interfaces.py 全部写接口走 `get_current_admin`），但沙箱存在的意义正是防"管理员从别处复制来的脚本藏毒"——这是最后一道防线，目前形同虚设 |
| 修复 | ① `_build_script_globals` 里显式 `g["__builtins__"] = dict(_SAFE_BUILTINS)`（exec 不再注入真 builtins）；② 校验器增加 `ast.Name` 黑名单（`__builtins__`、`__import__` 等 dunder 名一律拒）；③ 补一条"逃逸脚本必须被拦"的回归测试 |

### 🔴 2. 登录失败锁定可被 X-Forwarded-For 绕过 → 卡密可无限爆破

| 项 | 内容 |
|---|---|
| 位置 | `api/auth.py:35-39`（`_client_ip`） |
| 根因 | 无条件信任 `X-Forwarded-For` 头。攻击者每次请求换一个伪造 IP，按 IP 计数的"失败 3 次锁 1 小时"（`_attempts`）永远计不到同一个人头上 |
| 影响 | 锁定机制失效。验证码仍在（每次要过验证码），但锁定这层等于没有 |
| 修复 | 与 `admin_lan_only` 中间件保持一致：直接用 `request.client.host`，不看 XFF（当前是直连部署，没有可信反代；将来加反代时再配 trusted proxy 白名单） |

---

## 二、中危：逻辑 Bug（12 个）

### 状态机/数据一致性

**3. PATCH 卡密改到期日不复活 expired 状态 → 死卡**
`api/admin.py:261-270` 只改 `expires_at` 不动 `status`；而 `card_helpers.py:25-26` 对 `status=="expired"` 恒判过期。过期卡被改到未来日期后仍是死卡（复活逻辑只在 renew 接口里有）。修复：PATCH 改 `expires_at` 到未来时同步把 `expired` 改回 `used/active`。

**4. days 卡可生成 `valid_days=None` → 永久卡**
`api/admin.py:189`：days 类型不强制 `valid_days`；`card_helpers.py:32` `not card.valid_days → return False`（永不过期）。PATCH 也接受 `valid_days=0`（同样效果）。修复：days 类型生成/PATCH 时强制 `valid_days >= 1`。

**5. 删除/恢复任务只挡 `running`，不挡 `cancelling` → 幽灵任务复活 + 双协程并发**
`api/download.py:602`（delete）、`:634`（resume）：`cancelling` 状态可删/可恢复。删除后下载协程仍持有同一 dict 继续跑，后续 `persist_task` upsert 把刚物理删除的行重新插回（任务复活）；resume 则会在同一 task dict 上起第二个 `_run_batch_task` 协程，两个协程并发写 `completed`/`failed_list`。`interfaces.py:549` 同样只挡 running。修复：状态判断改为 `in ("running", "cancelling")`。

**6. 卡密过期清理不清内存任务**
`app.py:52-67` `_card_expiry_loop` 只 `mark_expired` + `clear_card_data`（清 DB），`_batch_tasks` 里该卡密的下载协程**继续运行、继续写盘**。修复：清零时遍历 `_batch_tasks` 把该卡密任务置 `cancelled`。

**7. WebSocket 扫码不校验卡密禁用状态**
`api/accounts.py:88-100`：只查 `Session.is_active` + 绑定，没走 `deps._auth_card` 的 disabled/expired 检查。禁用卡的会话不会被停用 → 被禁用卡密仍能扫码绑定新账号（expired 因 `mark_expired` 顺带清会话被间接覆盖，disabled 没有覆盖）。修复：WS 鉴权复用 `_auth_card`。

### 资源/并发

**8. 扫码登录同步 requests 阻塞事件循环**
`api/accounts.py:34/68/78` + WS 循环：`check_scan_status`/`generate_qrcode`/`verify_login` 是同步 requests（timeout=15），在 async 路由里直接调用。源站慢时整个事件循环卡 15 秒（所有用户请求都排队）。修复：`asyncio.to_thread` 包裹。

**9. 限流冷却期时区错乱**
`core/account_manager.py:161` 写入 UTC aware datetime，`:177/193` 读出 naive 后直接 `.timestamp()`（按本地时区解析）。UTC+8 下 24 小时冷却会**提前 8 小时失效**。讽刺的是 `card_helpers.py:14-20` 已有现成的 `_aware()` 就是干这个的，account_manager 没用。修复：读出后统一 `_aware()` 或改存 Unix 时间戳。

**10. 日志内存队列跨线程写 asyncio.Queue**
`api/log_router.py:40-44`：`emit` 会被下载线程池等任意线程触发，直接 `put_nowait` 非线程安全的 `asyncio.Queue`，可能丢唤醒或损坏队列内部状态。修复：捕获主循环引用，用 `loop.call_soon_threadsafe(q.put_nowait, entry)`。

**11. 脚本接口 worker 池泄漏**
`core/script_engine.py:691-693` + `core/interface_manager.py:397-432`：`shutdown` 只对 transient 池生效；`reload/reload_all` 替换旧 ScriptAdapter 时不释放其常驻 worker 池。每编辑保存一次脚本接口，就遗留至多 4 个常驻子进程。修复：reload 时对旧实例调 shutdown。

**12. 官方下载器 session.get 无超时**
`core/downloader.py:295`（元数据）、`:350`（流式下载）均无 `timeout`，源站挂起时工作线程永久阻塞（batch_runner.py:66 有 120s，这里漏了）。修复：补 timeout（元数据 15-30s，下载连接 30s/读取分段）。

**13. 二维码会话 TTL 过期后重建不回写**
`core/login.py:72-75`：过期后 `_qr_session` 新建 session（新 device id、丢 WAF cookie）但**不写回 `_QR_SESSIONS`**，之后每次轮询再建一个，`extract_cookies` 读的仍是已弹出的旧缓存 → 扫码必失败。正常流程（WS 180s < TTL 360s）碰不到，长挂 HTTP 轮询会必现。修复：过期直接返回"二维码已失效"，不要惰性重建。

**14. 插件每分钟对账把全队列 chrome.downloads.search 关在队列锁内**
`extension/background.js:228-257`：`reconcileDownloads` 在 `withQueue` 锁内对**每条**带 downloadId 的集串行 await 查询——包括已 done 的集（记录已被 erase，永远查不到也永远再查）。千集队列每分钟上千次串行 API 调用全程占锁，阻塞所有控制消息与状态更新。修复：① done/error 终态集直接跳过不查；② 查询移出锁（锁内快照 → 锁外查询 → 再加锁应用）。

---

## 三、低危 / 优化项（16 个）

| # | 位置 | 问题 |
|---|---|---|
| 15 | `api/admin.py:203-218` | `list_cards` 不校验 page/page_size，SQLite 负数 LIMIT=无上限，`page_size=-1` 全表返回（仅管理员+局域网，风险低） |
| 16 | `core/downloader.py:169-172,159` | `_get_driver` 快路径无锁读写 `_drivers`/`_driver_last_used`，与驱逐遍历并发可抛 "dictionary changed size"；`_evict` pop 掉锁后他线程可新建第二把锁，同账号串行隔离短暂失效 |
| 17 | `api/deps.py:31-32` | 每次鉴权都 UPDATE `last_active_at` + commit，每请求一次写事务，可加节流（如 60s 内不重复写） |
| 18 | `api/auth.py:79-90` | `_register_fail`/`_check_lock` 读写 `_attempts` 未持 `_state_lock`（只有验证码操作加了锁），并发计数竞态 |
| 19 | `api/extension.py:63-99` | 幂等去重先查后插无唯一约束，并发推送同一任务可双写（概率极低） |
| 20 | `api/files.py:105-110` | `parent != card_dir` 用 resolve 后的 parent 比未 resolve 的 card_dir，路径形式不一致时误判 |
| 21 | `api/admin.py:62-76` | 管理员登录无验证码/无限流，与卡密登录三重保护不对称（仅 LAN 中间件兜底） |
| 22 | `extension/background.js:631` | keepAlive `periodInMinutes: 0.05`（3 秒）低于 Chrome 下限被静默钳制为 ~30s，保活强度名不副实（实际仍勉强够用） |
| 23 | `extension/popup.js:220` + `background.js:480` | 弹窗每 1.5s 全量拉整个队列（含千集明细）；`handleDownloadChanged`/`updateTrack` 每次全量写整个队列——双重写入/传输放大 |
| 24 | `extension/manifest.json:15-17` | `dws1.6.8.js` 以 `<all_urls>` 暴露为 web_accessible_resources，任意网页可探测插件存在（offscreen 本地加载并不需要此项） |
| 25 | `frontend/src/utils/request.js:57-77` | 401 拦截器只清 localStorage 不同步 Pinia store，守卫短时间内仍判已登录（API 层有兜底会再跳，属 UX 瑕疵非越权） |
| 26 | `frontend/src/utils/sse.js:15-17` | 注释称"401 由 axios 拦截器统一跳转"，但 SSE 走 fetch 不经 axios → token 失效时任务页静默假死 |
| 27 | `frontend/src/views/Search.vue:105-108` | `selectAlbum` 无竞态防护，快速连点两张专辑时慢响应后写入，出现"标题 B 章节 A"错配（可加请求序号） |
| 28 | `frontend/src/views/admin/AdminCards.vue:183,191` | 搜索/筛选不重置 `page`，在第 3 页换新关键词常显示空表 |
| 29 | `db/session.py:35` | `PRAGMA foreign_keys=ON` 只对执行它的那条连接生效，连接池后续新建连接无外键约束 |
| 30 | `db/init_db.py:155-162` | accounts.json 迁移写入的行 `card_id=NULL`，而 account_manager 全部按 card_id 过滤 → 迁移数据对任何卡密不可见（迁移了个寂寞） |

---

## 四、死代码 / 冗余（清理清单）

| 位置 | 内容 |
|---|---|
| `core/sign.py` 整文件 | `generate_xm_sign` 全项目无调用方（签名逻辑已迁 Chrome 内执行） |
| `core/crypto.py:9` | `is_encrypted_url` 无调用方 |
| `core/script_engine.py:70-79` | `_ALLOWED_MODULES` 已弃用（黑名单模式后无意义），且 "hashlib" 重复出现两次；`_pool_key`（:598）仍把已弃用的 extra_modules 混进缓存 key，无意义分裂池 |
| `core/batch_runner.py:285-286` | 尾部 `finally: pass` 空分支 |
| `core/downloader.py:519-521` | `close()` 空实现（Chrome 池无统一清理钩子，进程退出时依赖 OS 回收） |
| `api/download.py:605` | 删除任务先 `persist_task("__delete__")` 写一行再物理删除，第一步是纯冗余写 |
| `frontend/src/views/Account.vue:5` | 从 `stores/auth` 导入不存在的 `getBizToken`（正确来源是 `utils/request`）——构建警告的根源，且导入后未使用，直接删 |
| `frontend/src/views/admin/AdminInterfaces.vue:84-91` | `DEFAULT_SCRIPT_CONFIG` 定义后无人引用 |
| `frontend/src/App.vue:2` | `ref, onUnmounted` 导入未使用 |
| `frontend/src/views/admin/AdminLogin.vue:19-24` | onMounted 重复 `probeAdminPath`（守卫刚探过，有 sessionStorage 缓存成本低，可省） |
| `extension/resolvers.js:72,90` | `RESOLVER_META` 导出后无消费方 |
| `extension/background.js:356-366` | `pendingTerminals` 对非插件下载的终端事件永久驻留内存，无清理路径 |

## 五、审查确认【无问题】的项（排除项）

- 插件 `withQueue` 全部 13 处回调逐一核查：**无嵌套调用，无死锁**
- `db/init_db.py` 列迁移幂等（PRAGMA 判断 + ALTER），重复启动不报错
- 插件 SW 重启恢复链（reconcile→autoApprove→pump）闭合；inflight/activeCount 在 finally 正确回收；消息类型与 popup 调用一一对应
- `core/script_examples.py` 被 `api/interfaces.py:364` 引用，非死代码
- 0.5.2 的 reconcile 修复（done 集不重置）逻辑正确

---

## 六、修复优先级建议

1. **立即**：#1 沙箱逃逸、#2 XFF 绕过（安全）
2. **尽快**：#3-#6 状态机一致性（死卡/幽灵任务/永久卡都是用户会踩到的）、#9 时区
3. **择机**：#8/#10/#11/#14 资源与性能、#17/#23 写放大
4. **随手**：第四节死代码清理（全是低风险删除）
