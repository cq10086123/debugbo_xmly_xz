# 修复方案：下载租约误判 / 多设备误报（不动下载主链路）

编写：2026-09-12　对应排查报告：`BUG_下载租约与多设备冲突_2026-09-12.md`
**本文件只是方案，未改任何代码。** 目标：消除 A1–A7 的误判与漏集，同时保证
「同一卡密全局单下载槽」「跨设备接管保护」「断点续传」「多卡密隔离」这四条既有语义**一字不改**。

---

## 0. 设计基线：什么绝对不能变（不变式清单）

| # | 不变式 | 现有实现依赖点 | 方案如何保证 |
|---|---|---|---|
| I1 | 同一 `card_id` 全局只有一个「进行中的下载」（本地插件 / 服务器任务共用一槽） | `card_download_locks.card_id UNIQUE` + `acquire` 条件 UPDATE/INSERT | 所有改动只**放宽「同一持有者自己」的续约**，绝不放宽抢占；`acquire` 的 SQL 一行不动 |
| I2 | 别的设备真的接管后，本机必须停止（防双份落盘） | 心跳 409 → `handleClaimLost` → `downloads.cancel` | 「明确丢失」仍走原逻辑；只有「通信不确定」才降级为**软停止**（不再派发新集，但不清状态、不冻结、不报「其他设备」） |
| I3 | 所有权凭证 = `local_tasks.claim_id` + `card_download_locks.claim_id` 双证一致 | `heartbeat/complete/cancel/xm-cookie` 校验 | 新增的「过期续租」必须**同时**满足两处的 `claim_id` 与 `task_id` 一致，任何人 claim 过都会破坏该匹配 → 自动失效 |
| I4 | 管理员 `force_release` 之后，心跳循环**绝不把槽抢回来** | `download_slot.server_task_heartbeat_loop` 注释、`admin.py:603-630`（强释时把 `local_tasks.claim_id` 置 NULL） | 服务器类型续约保持严格（不 reacquire）；本地类型续租因 `claim_id` 已被管理员清空而自动被拒 → 语义不变 |
| I5 | 断点续传：重 claim 保留已完成/进行中集的状态 | `background.js:406` 合并逻辑 | 只新增「取消类 error → 回 pending」白名单，真失败的集仍保留为 error（重试语义不变） |
| I6 | 多卡密互不可见 | `list_local_tasks` / 各端点均按 `card_id` 过滤 | 不改过滤；只**新增**返回字段（老客户端忽略未知字段，天然兼容） |
| I7 | 文件命名 / 去重 / `uniquify` / 官方账号轮询与冷却 | `buildFilename`、`resolveForTrack`、`_validate_active_claim` | 全部不动（A7 只改「我们自己取消」的识别，不改下载与命名） |
| I8 | DB 只做**可空增列**的轻量迁移，可原地升级、可回退旧版本 | `db/init_db.py:_migrate_*` 模式 | 见 §3（只加 1 列，且旧代码不读它） |

---

## 1. 分批与上线顺序

| 批次 | 内容 | 影响面 | 是否需要用户更新插件 |
|---|---|---|---|
| **P0（服务端，先行，收益最大）** | F1 心跳容错续租、F5 事件循环解阻塞、F7 释槽一致性、F8-D1/D4 | 只改服务端；老插件立刻少 80% 误报 | 否 |
| **P1（服务端 + 插件配套）** | F3 任务归属可见（`mine`/owner）、F2 插件心跳状态机、F4 取消豁免 | 插件不发版时，服务端行为向后兼容（老插件读不到新字段=按旧逻辑） | 是（0.7.2） |
| **P2（加固）** | F6 租约/TTL 自适应、B2 维护循环重写、D2/D3/D6/D8 | 低风险加固 | 是（与 P1 同包发） |

> P0 与 P1 可分两个 PR；每个改动都挂 `api_config` 开关（§4），出问题后台一键回退，不需要回滚镜像。

---

## 2. 逐项方案

### F1 ★ 心跳：把「基础设施异常 / 可恢复的过期 / 真失效」三者分开（修 A1+A2+A3+C3）

**问题回顾**：`download_slot.heartbeat` 只在 `lease_until > now` 时续约，DB 异常与「被拒」返回同一个 `False`；
`api/extension.py:310-332` 一律翻译成 409；插件 `background.js:441-468` 把 409/502/500/503 一视同仁地
「取消全部在飞下载 + 冻结 5 分钟」。实测：无人抢占、锁行仍属于自己的任务，仅仅过期 5 秒就被判死。

#### F1.1 `core/download_slot.py`：新增三态续约 + 异常类型

```python
class SlotUnavailable(RuntimeError):
    """基础设施不可用（DB 锁/超时/IO）。绝不许翻译成『租约失效』。"""

def renew(card_id: int, task_id: str, *, claim_id: str | None = None,
          holder_type: str = "local", allow_reacquire: bool = False,
          ttl_seconds: int | None = None, db=None) -> tuple[str, dict | None]:
    """续约租约。返回 'renewed' | 'lost' | 'requeued'（'requeued' 仅 local 且任务已被清扫）。

    与旧 heartbeat 的唯一区别：
      1) 去掉 `lease_until > now` 限制 —— 改为「锁行的 task_id 与 claim_id 仍属于本次持有者」即允许续租；
         过期但无人抢占 = 原地续命（幂等）；
      2) 锁行被 expire_stale 物理删除时，若 allow_reacquire=True 且本次 claim 仍是任务归属，
         走一次 acquire（其条件 SQL 天然保证并发下至多一个胜者）；
      3) DB 异常 raise SlotUnavailable，而不是 return False。
    """
    now = _now(); lu = _s(now + timedelta(seconds=ttl_seconds or ttl_for(holder_type)))
    own = db or SessionLocal()
    try:
        res = own.connection().exec_driver_sql(
            "UPDATE card_download_locks SET heartbeat_at=:now, lease_until=:lu "
            " WHERE card_id=:cid AND task_id=:tid "
            "   AND (:claim IS NULL OR claim_id IS NULL OR claim_id = :claim)",   # I3：不认别人颁发的凭证
            {...})
        if res.rowcount == 1:
            own.commit(); return "renewed", get_lock(card_id)
        if not allow_reacquire:
            return "lost", get_lock(card_id)
        ok, lock = acquire(card_id, holder_type, task_id, claim_id=claim_id, ...)  # 复用现有条件 SQL，未过期且他任务持有 ⇒ 必失败
        if ok:
            # I1 保证：只有「锁行不存在 / 已过期 / task_id 就是自己」才可能成功，不存在抢占
            return "renewed", get_lock(card_id)
        return "lost", lock
    except SlotUnavailable: raise
    except Exception as e:
        own.rollback(); raise SlotUnavailable(str(e)) from e
    finally:
        if db is None: own.close()
```

- `heartbeat()` 保留为薄封装（`renew(..., allow_reacquire=False)` → `(state=="renewed", lock)`），
  `app.py:_slot_maintenance_loop` 与 `server_task_heartbeat_loop` **不改签名**，I4 天然成立。
- `_p()` 解析失败由「视为过期」改为「视为有效 + `logger.error`」（修 C4 fail-open）；
  同时 `expire_stale()` 加 `AND length(lease_until) >= 19`，避免脏行被误删。

#### F1.2 `api/extension.py:310-349` `heartbeat_task` 重写（单事务 + 语义化错误码）

```python
@router.post("/tasks/{task_id}/heartbeat")
def heartbeat_task(task_id, req: HeartbeatRequest, auth=Depends(get_current_card)):   # 见 F5：async→def
    card_id = auth["card_id"]
    sess_hash = _session_hash(auth["token"])
    db = SessionLocal()
    try:
        t = db.query(LocalTask).filter_by(task_id=task_id, card_id=card_id).first()
        if t is None:
            return _err(410, "task_gone", "任务已被删除")
        if t.status == "pending" and not t.claim_id:
            # 被租约清扫打过回 pending：让插件「立刻重新 claim 同一本」而不是「冻结 5 分钟 + 报别人在下」
            return _err(409, "requeued", "任务已重新排队，请重新 claim 本任务")
        if t.status != "running" or t.claim_id != req.claim_id:
            return _err(409, "lost", "下载租约已失效，本任务已由其他下载持有")
        try:
            state, lock = download_slot.renew(
                card_id, task_id, claim_id=req.claim_id,
                holder_type="local", allow_reacquire=True, db=db)          # C3：与任务侧写同一事务
        except download_slot.SlotUnavailable:
            db.rollback()
            return _err(503, "busy", "服务器繁忙，请稍后重试",            # A3：不再是 409
                        headers={"Retry-After": "15"})
        if state == "renewed":
            now = _utcnow_naive()
            t.heartbeat_at = now
            t.lease_until = now + timedelta(seconds=download_slot.LOCAL_TTL_SECONDS)
            if req.progress is not None:        # F8-D1：顺带把进度写进同一事务（老插件不传 ⇒ 不覆盖）
                t.progress = json.dumps(req.progress, ensure_ascii=False)
            db.commit()
            return {"success": True, "lease_seconds": download_slot.LOCAL_TTL_SECONDS,
                    "lease_until": lock["lease_until"], "beat_seconds": _beat_seconds(),
                    "renewed_after_expiry": bool(lock.get("expired")),      # 观测点
                    "mine": True}
        return _err(409, "lost", "下载租约已失效，本任务已由其他下载持有")
    finally:
        db.close()
```

统一响应结构：`{"success":false,"code":"lost|requeued|task_gone|busy","error":文案}`。
`code` 是给插件做分支的；`error` 只给人看 —— 这样 A1 的「HTTP 状态码 = 语义」彻底解决。

#### F1.3 兼容性 & 风险

- 老插件（0.7.0/0.7.1）：只认 `success` 与 `error`，新增 `code/beat_seconds/mine` 被忽略 → 行为不变，
  但 A2/A3 已消失（不再被误判）。503 会被老插件当成「心跳被拒」→ 因此 **F1.2 的 503 分支在 P1 插件发布前先返回
  `success:true` + `degraded:true`**（见 F1.4，并用配置项切换），保证零回归。
- 安全边界：续租要求「锁行 claim_id 一致」或「任务 claim_id 一致 + 锁行不存在/自己持有」。别的设备一旦 claim 过同一任务，
  `_do_claim` 会写新 `claim_id` 并覆盖锁行 → 旧设备两条都不满足 → 仍被拒。**不新增任何越权面。**
- 不新增写放大：心跳仍是「1 UPDATE 锁 + 1 UPDATE 任务」，且合并为同一事务。

---

#### F1.4 ⚠️ 过渡策略（这条专门用来保证「不影响老插件的下载」）

老插件（≤0.7.1）把**任何非 200**都当「claim 失效」并立刻取消在飞下载（A1）。所以 P0 阶段不能把基础设施异常
改成 503 就完事——那只是把「误判为别人抢」换成「误判为服务器错误」，症状一样。因此：

```python
# 配置 heartbeat_infra_error_mode（api_config）
#   "success_degraded"（P0 默认）：DB/槽异常且『local_tasks 侧 claim 仍匹配』时，
#                                  只续任务侧租约，返回 200 + degraded:true + 计数告警
#   "http_503"（插件 0.7.2 普及后切换）：按 F1.2 返回 503，插件走 F2 的 soft-lost
```

- 为什么 `success_degraded` 是安全的：此刻所有权判定完全依赖 `local_tasks.claim_id == req.claim_id`（未变），
  若期间真有其他设备 claim 了同一任务，它会写入新 `claim_id` ⇒ 本机下一拍心跳立刻 `lost` 并停止 ⇒
  **最坏重叠窗口 = 一个心跳周期（≤60s）**，与今天「先误停再重排」相比，数据风险不增、体验大幅改善。
- 该模式下 `expire_stale` 不能顺手删掉「DB 异常但业务仍有效」的行——`SlotUnavailable` 分支不触发 `expire_stale`。
- 切换条件写进 CHANGELOG：0.7.2 覆盖 ≥ 90%（管理端按 `client_type='extension'` 的活跃会话版本分布看，
  插件在 `claim` 请求头/体里带 `plugin_version`，服务端记一条 `card_logs`，仅用于灰度决策）。

### F2 ★ 插件心跳状态机：按「剩余租约」决策，不确定时软停止（修 A1+A4+A6）

`extension/background.js`（0.7.2）

```js
// 心跳结果分类（抽成纯函数，可用 node --test 单测，见 §6）
function classifyBeat(res, leaseRemainingMs, serverCode) {
  if (res.ok && res.data?.success)            return 'renewed'
  if (res.status === 401)                     return 'relogin'            // 保持现状
  if (serverCode === 'requeued')              return 'requeue'            // ← 新增：立刻重 claim 同一本
  if (serverCode === 'lost')                  return 'hard-lost'          // 只有服务器明确说没了才硬停
  if (res.status >= 500 || res.status === 0 || res.status >= 429) return 'soft-lost'  // ← A1：不再等同失效
  return 'soft-lost'                          // 兜底：未知 ⇒ 保守软处理，绝不销毁本地状态
}
```

行为矩阵：

| 分类 | 停止派发新集 | `chrome.downloads.cancel` | 本地 status | frozen/errorMsg | 重连动作 |
|---|---|---|---|---|---|
| `renewed` | – | – | – | 清除 | – |
| `soft-lost` | ✅（`claimPaused=true`） | ❌ | 保持 `running` | ❌ 不写「其他设备」 | 退避 5/10/20/30s 重试，**只要剩余租约 > 0 就一直试** |
| `requeue` | ✅ | ❌ | `running`→`pending`（本地索引同步） | ❌ | 立即 `claimTask(同一 task_id)`，成功即原地续传 |
| `hard-lost` | ✅ | ✅（现状） | 按服务器列表判定 | 仅当 `mine===false` 才写「其他设备」 | 走 F3 的归属判定 |
| `relogin` | ✅ | ✅ | 不变 | 现状（提示重新登录） | – |

关键代码点：

1. `heartbeatClaim()`（`:441`）→ 用 `classifyBeat`；**只有 `hard-lost` 才调 `handleClaimLost('hard')`**。
2. `maybeHeartbeat()`（`:470`）加互斥 `let beating = null`（并发心跳复用同一 Promise）——修 A6 的 `beatFails` 双写；
   `beatFails` 不再独立触发放弃，改为「`now - lastBeatOkAt + 心跳间隔 > 剩余租约`」才放弃（用响应里的
   `lease_until` 换算，插件时钟不参与判定）。
3. 心跳请求加 `AbortController`（15s 超时）——避免 `pollBackend` 被一个挂死的 fetch 拖住整个 `withQueue` 链（B1 的插件侧对应物）。
4. `handleClaimLost`（`:478-522`）重写兜底分支（修 A4）：

```js
async function handleClaimLost(mode = 'hard') {           // mode: 'hard' | 'soft'
  …
  let sync = null                                          // ← 绝不复用陈旧的 lastServerTasks
  try { const r = await serverApi('/api/extension/tasks', {}); if (r.ok) sync = r.data?.tasks ?? null } catch {}
  if (mode === 'soft' || !Array.isArray(sync)) {           // 服务器不可达 / 拉不到列表
    task.frozen = false; task.claimPaused = true
    task.errorMsg = '与服务器同步中断，正在重试…'            // ← 不谎报「其他设备」，更不标 done
    if (claimState) await saveClaimState()                  // 保留 claim，恢复后可直接续租
    return
  }
  const st = sync.find(t => t.task_id === taskId)
  if (!st) {                                  // 服务器确实没有该任务（已终态/被删）
    const allTerminal = task.tracks.length && task.tracks.every(t => t.status === 'done' || t.status === 'error')
    if (allTerminal) { await completeClaim().catch(()=>{}) ; task.status = 'done' }
    else { task.status = 'pending'; task.errorMsg = '下载已被中断（服务器侧任务已不存在），可重新下载' }
    // ← 绝不再写 'done'
  } else if (st.status === 'running' && st.mine === false) { task.status='running'; task.frozen=true; task.errorMsg='其他设备正在下载此任务' }
  else if (st.status === 'running' && st.mine === true)  { /* 是我的：直接续 claim，见 F3 */ }
  …
}
```

5. `soft-lost` 期间 `pump()` 顶部加闸：`if (claimPaused) return`（不再派发，但已 in-flight 的集跑完 —— 不会与「重连后重新 claim 的同一本」并发写同一集，因为 `claimTask` 的合并逻辑与 `inflight` 集合保证单派发）。

**兼容性**：P1 插件对老服务器（无 `code` 字段）的行为 = `serverCode` 取 `res.data.error` 文本匹配 `/重新排队/` ⇒ 走 `requeue`，
其余非 200 一律 `soft-lost` ⇒ 最坏情况只是「不谎报、不取消、稍后重试」，不会改变下载结果。

---

### F3 ★ 让「谁在下载」成为事实而非猜测（修 A5 + D6）

#### F3.1 DB（唯一一处迁移，可空、旧版本可忽略）

`db/models.py::LocalTask` 增 1 列 + `db/init_db.py:_migrate_local_task_columns` 增映射：

```python
claim_session = mapped_column(String(32), nullable=True)   # 本次 claim 的会话指纹 sha256(token)[:16]，绝不存原 token
```

#### F3.2 服务端

- `_do_claim`：写 `t.claim_session = _session_hash(auth["token"])`，并在返回体里带
  `lease_seconds / beat_seconds / grace_seconds`（供 F2/F6 自适应）。
- `_task_to_dict(with_tracks, viewer_hash=None)` 增字段（**不返回 claim_id 本身**，避免同卡第二设备拿凭证去 `complete/cancel/偷 cookie`）：

```json
"claimed": true, "mine": true,
"owner": { "client": "extension", "ip": "192.168.100.23", "label": "插件 · 192.168.100.23",
           "heartbeat_age_seconds": 12, "lease_left_seconds": 287 }
```

  `mine = (t.claim_session == viewer_hash)`；`heartbeat_age_seconds` 让 UI 能区分「别人正在下」与「持有者早就离线」。
- ⚠️ **存量行兼容（这条决定升级会不会引入新误报）**：`claim_session IS NULL` 的 running 任务（升级瞬间仍在下载的老 claim、
  以及 0.7.1 之前发的 claim）必须判 `mine = True`（fail-open），与 `core/device_binding` 对历史 `device_id` 一律放行的策略一致。
  否则升级后插件会把**自己正在下的书**认成「其他设备在下」→ 当场冻结，等于制造新问题。判据写死为：
  `mine = (t.claim_session is None) or (t.claim_session == viewer_hash)`，并在 §6 用例 11 里锁定。
- `list_local_tasks` 用 `auth["token"]` 算 hash 传入；`skills.py` 的两处调用（`:487`、`:1172`）同步适配。
- **P2 可选**：`POST /api/extension/tasks/{id}/preempt`（body `{confirm:true}`）——
  `release(旧 task_id) + acquire(本设备)` 同一事务原子搬槽；旧设备下一拍心跳得 `hard-lost` 并停止。
  它不破坏 I1/I2（只是把「租约过期才能抢」提前成「同卡用户显式确认」），且管理端已有 `force_release` 作为对照实现。

#### F3.3 插件

- `pollBackend` 的 `frozen` 判定（`:814`、`:856-862`）改为 `st.status==='running' && st.mine === false`；
  `st.mine === true && !claimState` ⇒ 直接 `claimTask(taskId)` 找回（服务器 `renew` 幂等，见 F1），
  **这是「SW 重启 / claimState 丢失 / 401 重登」三类误报的根治点**。
- `retryFailed`（`:1404`）/`startTask`（`:1470`）的拒绝条件同样改用 `mine`；`mine===true` 时不再抛「请在该设备上重试」。
- frozen 卡片展示「持有者：插件 · 192.168.100.23（心跳 12s 前）」；`mine===false && heartbeat_age_seconds>180`
  时给「强制接管」按钮（P2）。
- D6：frozen 任务的「取消」改名「从本地列表隐藏」，并写入持久化 `xm_dismissed = {taskId: ts}`；
  `pollBackend` 步骤 1 跳过 `xm_dismissed` 里的 id（服务器侧该任务回到 pending 时自动解除）→ 不再「取消完又出现」。

---

### F4 ★ 我们自己取消的下载不得算作失败（修 A7，防漏集）

```js
// 1) 我们主动 cancel 前登记（handleClaimLost:494-517、cancelTask:1356-1372 两处）
const selfCancelled = new Map()          // downloadId -> ts，持久化到 xm_self_cancelled，TTL 5 分钟，上限 500
function noteSelfCancelled(ids) { ids.forEach(id => selfCancelled.set(id, Date.now())) ; saveSelfCancelled() }
function wasSelfCancelled(id) { /* 命中则返回 true（并清理）；SW 重启后从 storage 读 */ }

// 2) handleDownloadChanged:1266 判定改为
const wasCancelledByUser = task.status === 'cancelled'
  || wasSelfCancelled(id) || (delta.error && delta.error.current === 'USER_CANCELED')   // ← 豁免提到 state 分支

// 3) claimTask 合并逻辑:406：重 claim 时把「取消类」error 复位为 pending
const CANCELLED_ERR = /^(USER_CANCELED|ABORTED|已取消|下载租约过期|下载中断)$/
if (ot && ot.status !== 'pending') {
  const revived = ot.status === 'error' && CANCELLED_ERR.test(ot.error || '')
  …  revived ? { status:'pending', error:'', downloadId:null } : 原样保留
}
```

另外：`handleClaimLost` 的 cancel 与状态回写顺序改为「先 `noteSelfCancelled` → 再 `downloads.cancel` → 最后写 pending」，
消除 `pending → interrupted → error` 的竞态窗口。

---

### F5 ★ 服务端不再自伤：把阻塞 DB 挪出事件循环（修 B1+B2，这才是「多机一起用才偶发」的根因）

1. `api/extension.py` 全部生命周期端点 `async def` → **`def`**（FastAPI 自动进 threadpool；这些函数体已是纯同步 DB）：
   `create_local_task / list_local_tasks / claim_task / claim_task_by_id / heartbeat_task / complete_task / cancel_task / ack_local_task / get_xm_cookie / mark_xm_cookie_cooldown`。
   - 必配改动（否则 `TypeError: object dict can't be used in 'await' expression`）：`api/skills.py:487`、`api/skills.py:1172`
     的 `await list_local_tasks(...)` 改为 `await run_in_threadpool(list_local_tasks, auth=auth)`（或去掉 await）。
   - `api/extension.py` 全文**没有任何 `await`/`asyncio`**（已 grep 核对）⇒ 这 10 个端点可安全转 `def`。
   - **连带**：依赖 `get_current_card`（`api/deps.py:169`）与 `get_current_card_download`（`:183`）也转 `def`，
     并且**必须同时把 `_auth_card`（`api/deps.py:15`）转 `def`**——它现在是 `async def` 且被 `:180/:200` 两处 `await`；
     只转外层会在第一次请求就 `TypeError`。`_auth_card` 函数体同样纯同步（含 `HTTPException` 抛出，转 `def` 无影响）。
     其余 `async def` 端点（download/interfaces/skills）本批**不动**，避免牵动 SSE、`asyncio.create_task`、
     `asyncio.to_thread` 等真实异步逻辑（它们的同类改造放 P2，逐个函数评估）。
   - 转 `def` 后这些端点跑在 anyio 线程池（默认 40 线程）：`_batch_tasks`/`_intf_tasks`/`_episode_jobs` 是进程内普通 dict，
     读写都是单条字典操作（GIL 下原子），且既有协程只在协程线程里改同一个 dict 对象——无需加锁；
     新增的 `card_lock(card_id)`（下条）只保护「多步 claim/去重」序列。

2. 锁按卡分片，消除「一台设备拖住所有人」：

```python
_CARD_LOCKS: dict[int, threading.Lock] = {}
_LOCKS_GUARD = threading.Lock()
def card_lock(card_id): 
    with _LOCKS_GUARD:
        return _CARD_LOCKS.setdefault(card_id, threading.Lock())
# with card_lock(card_id):          取代 _CLAIM_LOCK / _TASK_CREATE_LOCK（全局单锁）
```
   并把 `create_local_task` 去重里的 `json.loads(tracks)` 比对挪到锁外（锁内只留 SELECT + INSERT）。
3. `_slot_maintenance_loop` / `_card_expiry_loop`（`app.py:62-134`）的整轮同步工作包进
   `await asyncio.to_thread(_slot_maintenance_tick)`，循环只负责 `asyncio.sleep`。
4. `sweep_expired_local_tasks` 重写为「同一事务：批量条件 UPDATE + 释槽」（修 C1，并去掉逐行 UPDATE）：

```python
ids = [r.id for r in db.query(LocalTask.id).filter(
        LocalTask.status=="running", LocalTask.lease_until.isnot(None),
        LocalTask.lease_until < _s(_now())).limit(200)]        # 单条 SQL 比较，格式与 _s 一致（已实测兼容）
if ids:
    db.connection().exec_driver_sql(
        "UPDATE local_tasks SET status='pending', claim_id=NULL, claim_session=NULL, claimed_at=NULL, "
        " heartbeat_at=NULL, lease_until=NULL, error='下载租约过期，任务已重新排队' "
        " WHERE id IN (%s) AND status='running'" % ",".join(map(str, ids)))
    db.connection().exec_driver_sql(                       # ← C1：回退与释槽同一事务
        "DELETE FROM card_download_locks l WHERE l.holder_type = 'local' "
        "  AND EXISTS (SELECT 1 FROM local_tasks t WHERE t.id IN (%s) "
        "              AND t.card_id = l.card_id AND t.task_id = l.task_id)" % ",".join(map(str, ids)))
    db.commit()
```
   （用 EXISTS 而非 `(card_id,task_id) IN (SELECT …)` 行值写法，兼容 NAS 上可能存在的旧 SQLite/Python 组合；
   `sweep` 之后不再依赖 `expire_stale()` 清残留；`expire_stale` 保留为表卫生。）
5. `skill_reset_stuck_local_task`（`api/skills.py:654-686`）补 `download_slot.release(card_id, task_id)`（C1 的第二处）。
6. P2 可选：`db/session.py` 的 `timeout` 30s → 8s，并给写路径加 `except OperationalError` 退避重试（3 次、jitter）；
   **必须与第 1 点同批或之后**，否则「事件循环里等 8s」比「等 30s」更容易连锁超时。

---

### F6 租约参数与心跳节奏对齐（修 B3，防止「配一个值就必现中断」）

```python
# core/download_slot.py
LOCAL_TTL_SECONDS  = max(180, int(os.environ.get("DOWNLOAD_SLOT_LOCAL_TTL", "600")))   # 300→600：容忍一轮 alarm 抖动 + 一次 GC
LOCAL_HEARTBEAT_SECONDS = max(15, LOCAL_TTL_SECONDS // 10)                              # 下发给插件的建议节奏
assert LOCAL_HEARTBEAT_SECONDS * 4 <= LOCAL_TTL_SECONDS      # 启动即校验，杜绝「TTL < 心跳间隔」的可配陷阱
```
- `claim`/`heartbeat` 响应统一带 `beat_seconds`、`grace_seconds = LOCAL_TTL//2`；插件按 `beatMs = clamp(beat_seconds*1000, 20s, 60s)`
  驱动：`chrome.alarms.create('beat', {periodInMinutes: beatMs/60000})`（Chrome 会钳到 ≥30s）+ 保留 1 分钟 `poll` 兜底
  + `handleDownloadChanged` 任意 delta 随路触发（沿用 `maybeHeartbeat` 节流）→ 长单曲也不会饿死。
- 权衡（写进注释）：TTL 600s ⇒ 崩溃遗留锁最长占用 10 分钟；用 F3.2 的 `preempt` + 管理端 `force_release` 兜住，
  并把 `expire_stale()` 周期从 30s 降到 10s，保证「过期即可被重排」不受 TTL 影响。

### F7 释放与状态一致性（修 C1+C2）
- `complete_task` / `cancel_task` / `ack_local_task`：`ok = download_slot.release(...)`；
  `ok is False` 时**再查一次** `get_lock`，若仍被本 task 持有 ⇒ 重试一次；仍失败则响应带
  `slot_released:false` 并 `logger.error`（插件收到后延迟 5s 再 claim 下一本，避免 409 空转）。
- `complete` 与 `sweep` 的竞态：现在依赖 `status='running' AND claim_id IS :cid` 条件写入（已有，保留），
  F5.4 的批量 SQL 保留同样的 `AND status='running'` 守卫 ⇒ 「已完成的书被打回 pending 重下」不会复活。

### F8 附带修复（小、独立、可单列 PR）
| 项 | 改法 | 风险 |
|---|---|---|
| D1 本地任务进度恒为 0 | `HeartbeatRequest` 加 `progress: dict \| None`、`current_title: str \| None`；F1.2 同事务写入；插件 `heartbeatClaim` 带 `progress`（`recomputeProgress` 已有） | 老插件不传 ⇒ `None` ⇒ 不覆盖，零回归 |
| D2 5000 集静默截断 | 响应加 `truncated: n`，前端 `Search.vue` push 成功提示里带「超出上限已截断 N 集」；`> 5000` 时同时返回 `success:true, warning` | 纯新增字段 |
| D3 服务器任务 id 32bit 全局撞车 | `task_id` 生成处加 `while tid in _batch_tasks: tid = new()`（`_intf_tasks`/`_episode_jobs` 同）；长度仍 ≤16，`CardDownloadLock.task_id String(16)` 不动 | 无 id 格式变更，UI/SKILL 不受影响 |
| D4 `card.expire_time` 不存在 | `skills.py:696-699` 改用 `is_expired(card)` + `card.expires_at`/`valid_days`；返回体键名保持 `expire_time`（SKILL 契约不变） | 修完该接口从「必报错」变成可用 |
| D5 `server_task_heartbeat_loop` 死代码 | 删除；或改为 `ttl_for(holder_type)` 并由 `app.py` 复用（**二选一，勿两存**） | 删的是未引用函数 |
| D8 解析挂死占住 `inflight`/槽 | `resolveForTrack` 外包 `withTimeout(…, 90_000)`；超时按一次普通失败计入 `maxRetry` | 只加超时，不改解析逻辑 |
| C2/观测 | 管理端「下载锁」页（已有 `list_locks`）增列：`expired 持续秒数`、`renewed_after_expiry 次数`、`mine 分布`；日志 `logger.info("[slot] renewed-after-expiry card=%s task=%s")` | 只读展示 |
| D9 灰度依据 | 插件 `claim` 请求体加 `plugin_version`（字符串，来自 `chrome.runtime.getManifest().version`）；服务端只写进 `card_logs` 一行，不参与任何判定 | 纯观测。老插件不带该字段 ⇒ `None`，逻辑分支不受影响 |

---

## 2.9 改动文件清单（爆炸半径一览，实现时按行勾选）

| 文件 | 改动 | 波及的既有功能 | 回退方式 |
|---|---|---|---|
| `core/download_slot.py` | 新增 `SlotUnavailable` + `renew()`；`heartbeat()` 退化为薄封装；`_p()` 改 fail-closed；`sweep_expired_local_tasks()` 改批量 SQL + 同事务释槽；TTL 常量下限；删死码 `server_task_heartbeat_loop` | 仅租约层。`acquire/release/get_lock/list_locks/force_release/expire_stale/cleanup_on_startup` 签名与语义**不变**（`tests/test_download_slot_unit.py` 现有 10 项必须原样绿） | `slot_renew_relaxed=0` ⇒ `renew` 内部退回严格谓词 |
| `api/extension.py` | `heartbeat_task` 重写（F1.2）；`_do_claim` 写 `claim_session` + 响应加 `beat_seconds/grace_seconds`；`_task_to_dict` 加 `mine/owner`（**不返回 claim_id**）；`complete/cancel/ack` 检查 `release` 结果；10 个端点 `async def`→`def` | 插件协议（只新增字段，旧插件忽略未知字段）；`/xm-cookie`、`/xm-cookie/cooldown` 的 5 项校验链**一行不改** | `extension_threadpool=0` 时保留 `async def` 版本（两版并存一个版本周期再删） |
| `api/deps.py` | `_auth_card` / `get_current_card` / `get_current_card_download` 转 `def`（连带，见 F5.1） | 所有需登录端点：行为等价，仅执行线程变化；网络绑定 + 风控逻辑逐行不动 | 同上 |
| `app.py` | `_slot_maintenance_loop`、`_card_expiry_loop` 主体包 `asyncio.to_thread`；`expire_stale` 拆成 10s 小循环 | 后台维护节奏；`cleanup_on_startup()` 与三个启动恢复函数**不动** | 去掉包装即可（1 行） |
| `db/models.py` + `db/init_db.py` | `LocalTask.claim_session` 可空列 + `_migrate_local_task_columns` 一行 | 无（新列，旧代码不读） | 残留列无害，无需回滚 DDL |
| `api/skills.py` | 2 处 `await list_local_tasks` 适配；`reset_stuck_local_task` 补 `release`；`get_card_info` 修 `expire_time` | SKILL 只读视图 + 一个诊断动作 | 独立小 PR，可单独回退 |
| `extension/background.js`（0.7.2） | `classifyBeat` 纯函数；`heartbeatClaim/maybeHeartbeat` 重写；`handleClaimLost(mode)`；`selfCancelled` 集合 + `handleDownloadChanged` 豁免；`claimTask` 合并白名单；`frozen` 改用 `mine`；`xm_dismissed`；`beat` alarm + `AbortController`；`resolveForTrack` 超时 | **下载主链路只加超时与豁免**：`pump/runTrack/resolveOfficialRotation/buildFilename/去重守卫/offscreen 签名` 不改 | 用户重载 0.7.1 即回退；服务端保持向后兼容（`code` 缺失 ⇒ 老插件按旧文本匹配） |
| `extension/popup.js` | frozen 卡片显示持有者与「心跳 N 秒前」；「取消」→「从本地列表隐藏」；（P2）强制接管按钮 | 纯展示 | – |
| `frontend/src/views/Search.vue` | 截断提示（D2） | 一行 toast | – |
| `Dockerfile` / 镜像 / 依赖 | 无改动（`requirements.txt` 不变） | – | – |

> P0（服务端 F1+F5+F7+D1/D4）**完全不碰 `extension/`**：老用户不更新插件也能拿到最大一块收益，
> 这是「不影响原有下载功能」的主要保障；P1 的插件改动全部落在「判定 + 展示」层，不改解析/下载/落盘/重试实现。

---

## 3. DB 变更（全部可空增列，原地升级）

| 表 | 变更 | 迁移函数 | 旧代码/旧版本影响 |
|---|---|---|---|
| `local_tasks` | `claim_session TEXT NULL` | `_migrate_local_task_columns` 加一行 | 旧版本不读该列；回滚旧镜像不需 DROP（残留列无害） |
| `api_config` | 新增 6 个键（§4） | 无需迁移（键值表） | 缺键 ⇒ 代码内默认值 |
| `card_download_locks` / 其余 | **无** | – | – |

不加索引：`local_tasks.lease_until` 已带 `index=True`（`db/models.py:322`），`status` 无索引但谓词在
`lease_until` 索引筛出的行上做过滤，配合 `LIMIT 200` 足够；`claim_session` 只按主键行读写，不需要索引。

## 4. 配置开关（`api_config`，后台「系统配置」勾选即时生效，零重启）

**只有下表"状态=已实现"的键在代码里真的被读取**；其余是规划项，配了也不会有任何效果（别拿它们当回退手段）。
读取走 `core/device_binding._load_cfg_cached`（5s 缓存，读全表，DB 异常回退默认值）；后台保存会立刻 `invalidate_cfg_cache()`。

| key | 默认 | 状态 | 作用 / 关掉 = |
|---|---|---|---|
| `slot_renew_relaxed` | 1 | ✅ 已实现（后台可勾选） | F1.1 过期后同凭证原地续租 + 锁行被清扫后的同任务重新登记。**关掉 = 心跳路径回到 0.7.1**（一个键同时管这两个兜底，避免"半新半旧"的组合语义） |
| `heartbeat_infra_error_mode` | `success_degraded` | ✅ 已实现（后台文本框） | 下载槽读不到时心跳怎么回。改成 `http_503` 前请先确认插件 ≥0.7.2；非法值自动回退默认 |
| `local_lease_seconds` | — | ⏳ 未实现 | 租约目前只由环境变量 `DOWNLOAD_SLOT_LOCAL_TTL`（默认 300，下限 30）决定 |
| `slot_renew_reacquire_local` | — | ⏳ 不需要 | 已并入 `slot_renew_relaxed`（见上） |
| `progress_on_heartbeat` | — | ⏳ PR-5 | D1 心跳顺带写 progress |
| `extension_threadpool` | — | ⏳ PR-3 | F5.1 端点转同步 `def` |
| `local_task_preempt` | — | ⏳ PR-6 | F3.2 强制接管端点 |

## 5. 兼容性矩阵（发版顺序的唯一依据）

| 客户端 | 只上 P0（服务端） | 上 P0+P1（含插件 0.7.2） |
|---|---|---|
| 插件 0.6.x（`ack` 流程） | `ack` 端点保持 `def`+原语义（F1 不触碰 `ack`）⇒ 无变化 | 无变化 |
| 插件 0.7.0 / 0.7.1 | A2/A3 误判消失；A5 误报仍在（502/超时仍会被它当「心跳被拒」——这正是 F1.4 要求 `heartbeat_infra_error_mode=success_degraded` 的原因：**新增的 503 与 `requeued` 两个响应在 PR-4 插件发布前不得启用**，否则只是把误报换个来源）；A4/A7 仍在 | 全量修复（F2 分类就绪后才允许开 503 / `requeued`） |
| 插件 0.7.2（未更新到 0.7.2 的用户） | – | 老插件 = 上一行行为，不会更差 |
| 网页端 | 无接口变更（`/api/extension/*` 只有新增字段） | 多一个「截断」提示 |
| SKILL | D1/D4 修复 ⇒ `check_local_tasks` 进度开始变化（0% → 真值）。这是**行为改进**，但需在 CHANGELOG 注明，避免调用方按「恒 0」写过断言 | 同 |
| 管理端 | `download-locks` 面板新增只读列 | 新增「强制接管」仅插件侧 |

## 6. 回归测试（必须新增，且现有用例全绿才算过）

`tests/test_download_slot_renew.py`（单元，monkeypatch `download_slot._now` 造时钟，不 sleep）
1. 过期 5s + 锁行仍是自己 → `renew` = `renewed`；旧 `heartbeat` 仍 False（锁死 I1：只放宽同持有者）
2. 过期 + 锁行 `claim_id` 属于他人 → `lost`
3. 过期 + 锁行被 `expire_stale` 删除 + 任务 `claim_id` 匹配 → `renewed`（reacquire 成功）
4. 过期 + 锁行被删 + 另一 task 已持槽 → `lost`，且**另一 task 的锁未被改动**
5. `holder_type='server'` + `allow_reacquire=False` → 过期即 `lost`（管理员 `force_release` 后抢不回来，I4）
6. DB 异常 ⇒ `SlotUnavailable`（用打桩 `exec_driver_sql` 抛 `OperationalError`）
7. `_p()` 脏值 → 视为有效（C4）

`tests/test_download_slot_e2e.py` 追加（TestClient + 双卡密）
8. A2 现场复现回归：两卡并行，人为推后 A 的租约 5s → A 心跳 200 且 `renewed_after_expiry=true`；B 不受影响
9. `requeued`：过期 → `sweep` → 心跳 409 `code=requeued` → 同 task 立刻 claim 成功、`claim_id` 轮换、旧 `claim_id` 心跳被拒
10. 管理员 `force_release` 后本地插件心跳必须 `lost`（不得续租）
11. `/api/extension/tasks` 返回 `mine/owner`，且**不含 `claim_id`**；A 设备的 token 无法用 B 设备的 claim 调 `complete`
12. D1：心跳带 progress → `local_tasks.progress` 落库；不带 ⇒ 保持旧值
13. C1：`skill_reset_stuck_local_task` 后同卡立刻 `POST /api/download/batch` **不再 409**
14. F5.4：`sweep` 单条 SQL 与 `complete` 并发（线程）→ 不出现「done 被打回 pending」
15. 多卡并发 20 卡 × 100 次 claim/heartbeat/release 交叉：断言「同卡至多一个持有者」全程成立（I1 压测）

插件侧（无框架 ⇒ 把判定抽成纯函数）：新增 `extension/tests/beat_policy.test.mjs` + `node --test`
16. `classifyBeat` 全矩阵（502/0/429/401/409-lost/409-requeued/200）
17. `wasSelfCancelled` + `claimTask` 合并：`USER_CANCELLED` 的 error 集在重 claim 后回 pending，真失败集仍为 error（防 A7 漏集）
18. `handleClaimLost('soft')` 绝不产生 `status='done'`，且列表拉取失败时保留 `claimState`

手工验收（NAS 现场）
19. 下载中 `docker restart` 容器 30s / 3min → 期望：日志「与服务器同步中断，正在重试…」，**不出现**「其他设备正在下载」，恢复后原地续传，集数不重不漏
20. 合盖睡眠 10 分钟再唤醒 → 期望：重新 claim 同一本并续传（不是冻结 5 分钟）
21. 两台电脑两张卡各下一本 200 集并发 2 小时 → 期望：两份 `local_tasks` 进度均单调递增、无 409、落盘文件数 = 集数（`find … -name '*.mp3' | wc -l`）

## 7. 观测（改完要能量化，而不是「感觉不报了」）
- 计数指标（`logger.info` + 管理端面板）：`renewed_after_expiry`、`soft_lost_total`、`requeue_total`、
  `slot_unavailable_total`(503)、`db_locked_retry`、`sweep_requeue`、`frozen_total`。
- 期望：上线一周后 `slot_unavailable`/`soft_lost` > 0 但 `hard-lost` ≈ 0（真接管才应有 hard-lost）。
- 验收红线：任何一台设备的 `local_tasks.status='running'` 与 `card_download_locks.task_id` 不一致时长 > 60s 即视为回归。

## 8. 明确不做（避免牵一动百）
- 不引入 Redis/独立锁服务、不改 `card_download_locks` 主键与唯一约束、不做破坏性迁移。
- 不加 WebSocket/SSE 推送 claim 状态（沿用轮询，只把字段补全）。
- 不改「同一卡密全局单下载槽」产品语义（不做「多设备并行下同一本」）。
- 不动文件命名 / 去重守卫 / `uniquify` / 官方账号轮询与冷却逻辑。
- 不改 SKILL 与网页端既有字段语义（只新增）。

---

## 9. 落地顺序（每个 PR 都能独立上线、独立回退）

| PR | 内容 | 上线后立即可见效果 | 自检 |
|---|---|---|---|
| **PR-1 ✅ 已落地（2026-09-12）** | `db/models.py`+`init_db` 加 `claim_session` 可空列；`_do_claim` 写入该列；`_task_to_dict` 输出 `mine`（NULL ⇒ true）/`owner`；不返回 `claim_id` | 无行为变化（纯新增字段），为后续铺路 | `tests/test_download_slot_e2e.py` 全绿；手工看 `/api/extension/tasks` 响应多字段 |
| **PR-2 ✅ 已落地（2026-09-12）** | F1.1 `renew()` + `SlotUnavailable`；F1.2 `heartbeat_task` 重写 + `code`；F1.4 `heartbeat_infra_error_mode=success_degraded`；F7 释槽校验 | 「无人抢占却过期 5s」不再中断（用例 8）；`requeued` 让插件原地重 claim，不再冻结 5 分钟 | 新增 §6 用例 1–9；老插件回归（下载中重启容器 30s → 不再出现两句提示） |
| **PR-3（服务端·性能）** ⬜ 未开始 | F5.1–F5.4 + `api/deps.py` 转 `def`；每卡一把锁 | 多卡并发时心跳延迟毛刺消失（A3 的另一半） | 20 卡 × 100 次交叉压测（用例 15）；`/api/download/tasks` SSE 不受影响 |
| **PR-4（插件 0.7.2）** ⬜ 未开始 | F2 状态机 + `AbortController` + `beat` alarm；F4 `selfCancelled` + 合并白名单；F3.3 `mine` 判定 + `xm_dismissed`；F6 节奏自适应 | 「其他设备正在下载」只剩真实接管；中断续传不漏集；frozen 任务可隐藏 | §6 用例 16–18；现场验收 19–21 |
| **PR-5（附带）** ⬜ 未开始 | D1 心跳带 progress、D2 截断提示、D3 id 撞车守卫、D4 `expire_time`、D5 删死码、D8 resolve 超时 | SKILL 能看到本地任务真实进度；`get_card_info` 可用 | 新增 3 个 SKILL 用例 |
| **PR-6（可选·体验）** ⬜ 未开始 | `preempt` 强制接管 + `AdminConfig` 里加「离线多久可强制接管」阈值 + 后台下载锁面板增强 | 用户不必等 10 分钟租约自然过期 | 用例 4（接管后旧设备必须被拒） |

> 切换 `heartbeat_infra_error_mode = http_503` 放在 PR-4 发布满 2 周后，单独一次配置变更（无需发版）。

---

## 10. PR-1 + PR-2 实现说明（2026-09-12 落地记录）

只改服务端，`extension/` 一行未动 ⇒ 老插件行为完全不变，只是**收到的响应更容易**（少误判）。

### 10.1 落地的文件与符号

| 文件 | 改动 |
|---|---|
| `core/download_slot.py` | 新增 `SlotUnavailable`、`_lease_of()`（脏值 fail-closed）+ 公开别名 `lease_of()`、`_MIN_LEASE_LEN`、`renew()`（三态续约 + `allow_reacquire` + `db=` 同事务）、`acquire(db=, raise_on_error=)`、`release(db=, raise_on_error=)`、`get_lock(raise_on_error=)`；`heartbeat()` 改为 `renew(require_live=True, holder_type='server')` 的薄封装（签名/不抛异常语义不变 ⇒ `app.py`、`api/admin.py` 零改动）；`expire_stale()` 加 `length(lease_until) >= 19`；`sweep_expired_local_tasks()` 的 reset 追加 `claim_session = NULL` |
| `db/models.py` | `LocalTask.claim_session`（`String(32)`，可空，只存 `sha256(token)[:16]`） |
| `db/init_db.py` | `_migrate_local_task_columns` 增 `"claim_session": "TEXT"`（老库启动自动补列，存量行 NULL ⇒ fail-open） |
| `api/extension.py` | `_session_hash` / `_cfg` / `_renew_relaxed` / `_infra_error_mode` / `_beat_seconds` / `_api_error` / `_release_slot`；`_task_to_dict` 输出 `claimed`/`mine`/`owner`（**不回 `claim_id`**）；`_session_labels` 一次查询建指纹→展示信息映射；`_do_claim` 写 `claim_session` 并下发 `beat_seconds`/`grace_seconds`/`mine`；`heartbeat_task` 重写（`renewed`/`degraded`/`requeued`/`lost` + `slot_renew_relaxed` 开关 + `db=` 同事务）；`complete`/`cancel`/`ack` 检查释放结果并回 `slot_released`、清 `claim_session`；`/xm-cookie` 与 `/xm-cookie/cooldown` 在读不到锁时转 503 `code=busy`（带 `Retry-After`）；claim/complete/cancel 的 409 全部补 `code` |
| `api/admin.py` | `force_release` 副作用里补 `claim_session = None` |
| `frontend/src/views/admin/AdminConfig.vue` | 新增两个回退开关（`slot_renew_relaxed` 默认 1、`heartbeat_infra_error_mode` 默认 `success_degraded`）+ 生效默认值展示（库里无行时不显示成"关闭"）|
| `tests/` | 新增 `test_download_slot_renew.py`（8 例）、`test_extension_claim_owner.py`（10 例）；`test_download_slot_e2e.py` 见 10.3 |

### 10.2 与方案的两处差异（都是实现期发现后收敛的）

1. `renew()` 的 `allow_reacquire` 走的是 `acquire()` 的**同一条条件 SQL**（`db=` 复用调用方会话），不是新写一条 UPDATE —— 少一份谓词就少一处会漂移的地方。
   期间测出一个真 bug：`renew` 自持会话 + reacquire 成功时**没人 commit**（`acquire(db=own)` 被要求不自取提交），INSERT 随 `close()` 回滚；已修，测试 4 盯着。
2. `heartbeat_task` 里加了一个方案没写的观测位 `renewed_after_expiry`（本次心跳是否发生在"任务侧租约已过期"之后）。它是切换 `slot_renew_relaxed` 前唯一的度量：上线后统计这个比例，接近 0 说明放宽够用、不必再动 TTL。

### 10.3 为什么改了一条既有 e2e 断言（唯一的"动了老测试"处）

`test_download_slot_e2e.py::test_lease_expiry_sweep_and_reclaim` 原来断言"租约过期 ⇒ 心跳 409"，**这正是被修掉的 A2 误报本身**。处理方式：
- 该用例开头显式 `monkeypatch` 关掉 `slot_renew_relaxed` ⇒ 它现在验证"回退开关关闭 = 与 0.7.1 完全一致"；
- 新增 `test_lease_expiry_renews_in_place` 验证默认语义：过期但 claim 未变 ⇒ 200 原地续命，**同时必须**（a）别的任务仍被 409 挡住（单槽语义不变）、（b）`claim_id` 被改写后旧设备仍 409 `lost`（真接管照旧拦截）。

### 10.4 回退与验证

- 关掉 `slot_renew_relaxed` ⇒ 心跳/续约路径逐字节回到 0.7.1 行为（`tests/test_extension_claim_owner.py::test_relaxed_switch_off_restores_old_behavior` 锁死）。
- 回退整个 PR：`git revert` 本次提交即可；`claim_session` 是可空列，留着不影响老代码（读写它的只有本次新增逻辑）。
- 全量：`pytest tests/ -q` → **174 passed**（基线 155 + 新增 19 例；master 基线上那条 `test_skill_token` 合跑污染属既有问题，本次未引入）。

### 10.5 尚未开始（按 §9 顺序）

PR-3（`extension` 生命周期端点转同步 `def` + 每卡一把锁，方案 F5；`api/skills.py` 六处直接 `await` 端点必须同批改）、PR-4（插件 0.7.2 状态机 F2/F3.3/F4，`requeued`/`degraded`/`mine` 才真正被消费）、PR-5、PR-6。
> `heartbeat_infra_error_mode` 改 `http_503` 必须等 PR-4 发布满 2 周。

---

## 11. 仿真运行环境 + PR-2.6（2026-09-12 第二轮）

为了"能跑起来验证"而不是只在代码层面推理，加了一套可复跑的本地仿真（**不进镜像**，Dockerfile 只 COPY 指定目录）：

```
tools/sim/harness.py     起真服务（python app.py + 独立 DATA_DIR）→ 播种卡密/会话/接口 → 跑场景 → 断言 → 收口
tools/sim/run.py         假插件（复刻 background.js 的 claim/心跳/派发/收尾状态机）+ 故障代理（502/拒连/挂起/延迟）
tools/sim/README.md      场景清单与跑法
```

它和真环境的差别（结论时已考虑）：下载字节是假文件、无 Chrome API、代理在同机。所有**租约/claim/心跳/列表**交互都是真 HTTP + 真 SQLite + 真 `core/download_slot.py`。

### 11.1 场景与实测结果（`SLOT_TTL=45`，插件心跳 6s）

| 场景 | 结果 |
| --- | --- |
| S1 正常下载 30 集 | ✅ 30/30、零提示、零残留锁（**改动没碰坏正常链路**） |
| S2 反代 502 持续 40s（租约未过期） | 0.7.1 → 谎报「其他设备正在下载此任务」+ 掐断；0.7.2 → 不掐断，只提示真话「无法联系服务器，已暂停（恢复后自动继续）」，恢复后 60/60 |
| S3 服务进程 `kill -9` 后 20s 重启 | 0.7.1 → 「下载中断（租约失效）…」**且 19/50 被标成 done**；只改状态码判定（PR-2.5）**同样**中招；0.7.2 → 半本不再被标完成，恢复后 50/50 |
| S4 锁刚过期、sweep 还没跑（**用户报的主窗口**） | relaxed=1 → 无提示、继续下载；relaxed=0（等价 0.7.1）→ 「其他设备正在下载此任务」+ 掐断 |
| S5 同卡双设备 | B 被正确挡住 → A 离线 → sweep → B 接管 → A 回来被拦，**同集双写=无**（真接管保护完好） |
| S6 六卡密并发 + 心跳延迟 | 150 集全 done、零误报；写请求 p95=4ms（这个规模下没测出排队，见 §11.3） |
| S7/S8/S9 顺带查出来的数据完整性问题 | 全部先复现（S7/S8 首跑为红）→ 修完转绿（见 §11.2） |

判据是「不许谎报归属」而不是「完全不提示」：真联系不上时提示「已暂停，恢复后自动继续」是**正确行为**。

**S3 是本轮最值钱的发现**：证明方案 F2 只写「非 2xx 不判死」**不够**——`catch` 分支（连不上）到上限后仍会走 `handleClaimLost`，而列表也拉不到时用陈旧 `lastServerTasks` 推断 ⇒ **把半本标成 done 并上报服务器 complete**。这条已从"提示打扰"升级为"数据完整性事故"，并据此做出 PR-2.6。

### 11.2 顺带查出并修掉的四个真问题（都在 `POST /api/extension/task`）

| # | 现象（仿真实测） | 修法 |
| --- | --- | --- |
| N1 | 5003 集的书 → 只落库 5000 集，响应 `count=5000` 且 `success=true`，**没有任何地方说明少了 3 集** | 响应加 `requested` / `truncated`，超限时 `message` 直说「剩余 N 集未加入队列，请再推一次剩余区间」；幂等分支同样回带这两个字段 |
| N2 | 同一专辑「换格式再推一次」被幂等去重**吞掉**（去重只比曲目集合，不比 `fmt`）⇒ 用户等的是旧格式，服务器回 200 | 去重身份 = 曲目集合 **+ `fmt`**；仅 `quality` 不同时仍幂等，但回 `quality_diff: true` 并在 message 里说明「换音质不改本地文件名，已下载的集会被插件跳过」 |
| N3 | done 任务的 30 分钟去重窗口锚在 `created_at` ⇒ 下了 90 分钟的书**刚下完就失去保护**，双击/重推立刻产生重复任务（插件落盘成「书名 (1)」） | 锚点改为 `finished_at or created_at` |
| N4 | （同源隐患）`TrackItem.fmt` 有默认值 `"mp3"`，调用方不写 fmt 时任务级 `fmt=m4a` 也会让每集变 mp3；插件取后缀用 `tr.fmt \|\| task.fmt` ⇒ 按 m4a 解析、按 `.mp3` 存盘 | `create_local_task` 入库前只在**调用方未显式给** `fmt` 时继承任务级 `fmt`（`model_fields_set` 判定），显式给过的一字不改 |

### 11.3 PR-3 的判断修正

S6 只测出「6 卡 × 25 集」规模下写请求 p95=4ms——心跳每卡 6s 才一次，写入极轻，**这个量级证明不了 async 端点有问题**，也不能反过来证明没问题（真实事故是"下载洪峰 + 反代超时 + 共享 SQLite"三者叠加，仿真里我只造了第一项）。结论不变但优先级要诚实标注：**PR-3 仍是 F5 的正解，只是本地仿真无法为它提供收益证据**，要落地请直接在真实实例上量 `/api/extension/*` 的 p95 与 502 计数，而不是等仿真给答案。

### 11.4 PR-2.6（已发版，插件侧最小改动）

`extension/background.js` 0.7.1 → **0.7.2**，只动失败路径判定（5 处）：

1. 新增纯函数 `classifyBeat(res)`：`renewed / relogin / requeue / hard-lost / soft-lost`（注释里写明为什么分五类）；
2. `heartbeatClaim()` 按分类处置：只有 `hard-lost`（服务器明确 409/404）才 `handleClaimLost`；`requeue` **立刻重 claim 同一本**并继续；`soft-lost` 累计到 `BEAT_COMM_FAIL_LIMIT` 只 `markStalled()`（暂停派发，保留 `claimState` 与已下内容）；
3. `handleClaimLost()` 加 `listFresh`：列表拉不到 ⇒ **绝不** finalize（不标 done、不标「其他设备」），只清 claim + 暂停；
4. `pump()` 顶部 `if (beatStalled) return`（在飞下载不打断、已完成不丢，恢复后自动继续）；
5. 心跳节奏改用服务端 `beat_seconds`（S3.3 下发的 `beat_seconds/grace_seconds` 现在真被消费），连续失败时收紧到 15s 重试。

`api/extension.py` 的 `heartbeat_infra_error_mode` 保持 `success_degraded`：**PR-2.6 之后就可以按方案切 `http_503`**（插件已能区分基础设施异常），但收益只剩"少写一次 200 日志"，建议维持现状。

### 11.5 验证与回退

- 全量：`pytest tests/ -q` → **186 passed**（174 + `tests/test_extension_task_create.py` 5 例 + `tests/test_plugin_beat_classify.py` 7 例）。
- `tests/test_plugin_beat_classify.py` 的做法：**从 `background.js` 里按大括号配对抽出 `classifyBeat` 源码，用 node 真跑**，逐条比对决策表；另两条结构断言钉住「出口集合只能是这五种」「`pump()` 必须按 `beatStalled` 收口」「续约成功必须解除暂停」。改 JS 忘了改这里 → 红。
- `tools/sim/harness.py` 启动时会用 node 把同一张表跑一遍（`verify_against_plugin()`），保证假插件复刻的状态机与真插件源码不漂移。
- 回退：`git revert` 本轮提交即回到 PR-1/PR-2；插件侧无需服务器配合（老服务器只回 409/200，`classifyBeat` 对无 `code` 的 409 仍按 `hard-lost` 处理 ⇒ 保护不打折）。

---

## 12. 部署清单（重新部署前照着走一遍）

### 12.1 必须做的三件事

1. **前端要重新构建**：`frontend/dist` 不在 git 里，镜像是 `COPY frontend/` 后 `npm run build`（Dockerfile 第 8/12/49 行）。若你的部署流程用**已有 dist** 而不是重新 build，后台「系统配置」里那两个新开关不会出现——开关本身在服务端默认生效（`slot_renew_relaxed=1`），所以**不影响修复生效**，只影响"能不能一键回退"。
2. **数据库不用动手**：`claim_session` 由 `init_db()._migrate_local_task_columns()` 在启动时 `ALTER TABLE ... ADD COLUMN`（可空、无默认值），全仓迁移都是这套 `PRAGMA table_info` 写法 ⇒ **仅支持 SQLite**（`db/session.py` 也只有 sqlite URL）。停机一次拉起即完成，老数据不受影响。
3. **插件要更新到 0.7.2 才享受"不打断下载"的那一半**：服务器侧（PR-1/PR-2）单独上线就已经消灭了 `租约失效` 误报；但「联系不上 ⇒ 半本被标成 done」「陈旧列表 ⇒ 谎报其他设备」这两条是插件判定，只有 0.7.2 修掉。老插件（0.7.0/0.7.1）对新服务器**完全可用**（只认 409/200，`code` 是纯增量字段）。

### 12.2 上线后看什么（不要靠"感觉不报了"）

- `grep 过期后原地续租` / `renewed_after_expiry`：心跳响应里带这个观测位。数量接近 0 ⇒ 说明租约长度够用，不用再调 TTL；持续增长 ⇒ 该机器网络/磁盘确实慢，考虑把 `DOWNLOAD_SLOT_LOCAL_TTL` 调到 600。
- `grep "下载槽不可用，本轮降级放行"`：这是 DB 抖动的真实频率（服务器繁忙时应该看到它，而不是用户报障）。
- `grep "租约字段无法解析"` + "清理 N 条…孤儿下载槽"：出现就说明库被手工改过/从别处恢复过，需要人工核对那几个卡密。
- 后台「下载槽」列表：`lease_left_seconds`/`owner` 现在能直接看出"是谁占着槽"，不用猜。

### 12.3 回退路径（从轻到重）

| 级别 | 动作 | 效果 |
|---|---|---|
| 1 | 后台取消勾选 `slot_renew_relaxed` | 心跳/续约路径逐字节回到 0.7.1（有测试锁死）。**注意**：同时关掉"锁行被清扫后同任务重新登记"这个兜底 |
| 2 | `heartbeat_infra_error_mode` 保持 `success_degraded` | 别在插件 <0.7.2 时切 `http_503`（老插件会把 503 当失效，症状和误报一模一样） |
| 3 | 插件退回 0.7.1 | 服务器新字段自动被忽略，行为回到改动前（仍享有 PR-1 的"过期原地续租"收益） |
| 4 | `git revert` 本轮 + 上一轮两个提交 | 全量回到 master；`claim_session` 是可空列，留着不影响老代码，**不需要**删列迁移 |

### 12.4 本轮审查改了什么（第一性：不改下载主链路）

| # | 问题 | 处置 |
|---|---|---|
| R1 | `!listFresh` 分支被放在"按陈旧列表猜归属"之后 ⇒ **「其他设备正在下载此任务」的误报实际没修掉**（列表里那条 running 常常就是本机自己下的那本） | 处置逻辑抽成纯函数 `decideClaimLost()`，顺序钉死在测试里；仿真 S2/S3 故障前先把列表"拉热"，这样才测得到该路径 |
| R2 | `pump()` 里 `if (beatStalled) return` 会让用户在 popup 点「开始/继续/重试」**毫无反应**（暂停只能等下一个 alarm） | 暂停态下 `pump()` 改为后台补一次心跳探测（不 `await`，否则服务器"连而不答"会把派发链一起卡死），连上立刻解除并继续 |
| R3 | `serverApi` 无超时：一次挂起的连接能吊住整条 claim 状态机 | 加 `timeoutMs`（仅心跳传 20s，其它调用语义不变，由测试钉住），超时按 `soft-lost` 处理 |
| R4 | SKILL `reset_stuck_local_task` 只改任务状态、不释放下载槽 ⇒ 接口承诺的"30 秒内重新接管"实际要等满一个租约（300s） | 同时 `_release_slot()` + 清 `claim_session`，并把"没释放成功"写进返回文案（`slot_released` 字段）；新增 3 例回测 |
| R5 | 脏 `lease_until` 行 fail-closed 后，若它指向的任务已不存在 ⇒ 该卡密**永久** 409 | 新增 `reap_orphan_locks()`（维护循环调用）：仅"不可解析 + 心跳早于 3×TTL + 无进行中任务"三重条件才清；`length()<19` 谓词在 MySQL/PG 上天然 no-op，方言安全 |
| R6 | `claimTask` 靠**中文文案正则**区分"槽忙"与"任务不可 claim"（服务器改文案就会退化成无限重试） | 优先看 `code`（`already_claimed/not_claimable/task_gone/busy`），文案匹配只作老服务端兜底；`busy_response` 补 `code:"busy"` |
| R7 | 文档把 6 个 `api_config` 键都写成可用 ⇒ 运维可能去拨一个不存在的开关当回退 | §4 改成带"状态"列的表，并说明 `slot_renew_reacquire_local` 已被并入 `slot_renew_relaxed` |
| R8 | `_beat_seconds()` 在小 TTL 下可能只够 2 拍（一次抖动就过期） | 改成 `ttl//6` 并显式保证 `beat*4 <= ttl` |

验证：`pytest tests/ -q` → **190 passed**；`tools/sim` S1/S2/S3/S4/S5 全绿（判据见 §11.1、§12.4-R1）；`node --check extension/background.js` 通过；`verify_against_plugin()` 现在同时比对 `classifyBeat` 与 `decideClaimLost` 两张决策表。
