# 供体池验证与级联刷新功能实施说明

**实施日期**: 2026-08-24  
**实施人**: AI Assistant

---

## 📋 功能概述

本次实施为喜马拉雅供体池系统添加了两个核心功能：

1. **供体池批量验证** — 管理员可验证供体账号 cookie 的有效性，前端显示有效/失效/未验证状态
2. **Cookie 级联刷新** — 供体池 cookie 更新时，自动同步到所有已注入的工作表副本

---

## 🔧 技术实现细节

### 1. 数据库模型变更 (`db/models.py`)

**BackendXmAccount 表新增字段：**

```python
last_verified_at = mapped_column(DateTime, nullable=True)
is_valid = mapped_column(Boolean, nullable=True)  # True=有效, False=失效, None=未验证
```

**设计考虑：**
- `is_valid` 使用三态（True/False/None）区分"已验证有效"、"已验证失效"、"未验证"
- `last_verified_at` 记录最后验证时间，便于管理员判断验证时效性
- 字段全部可空，保证存量数据兼容

### 2. 数据库迁移 (`db/init_db.py`)

新增 `_migrate_backend_xm_columns()` 函数，为已存在的 `backend_xm_accounts` 表补充新字段：

```python
def _migrate_backend_xm_columns():
    needed = {
        "last_verified_at": "DATETIME",
        "is_valid": "INTEGER",
    }
    with _engine.connect() as conn:
        cols = {row[1] for row in conn.exec_driver_sql("PRAGMA table_info(backend_xm_accounts)").fetchall()}
        for name, ddl in needed.items():
            if name not in cols:
                conn.exec_driver_sql(f"ALTER TABLE backend_xm_accounts ADD COLUMN {name} {ddl}")
```

**迁移策略：**
- 使用 SQLite PRAGMA 检查字段是否存在，避免重复添加
- 启动时自动执行，无需手动迁移
- 字段可空，不影响现有数据

### 3. 核心逻辑变更 (`core/account_manager.py`)

#### 3.1 级联刷新逻辑 (`add_backend_account`)

当供体池账号的 cookie 被更新时，自动同步到所有已注入的副本：

```python
if old_cookie != cookie_str:
    cascade_count = db.query(XimalayaAccount).filter_by(
        injected=True, backend_src_id=b.id,
    ).update(
        {
            XimalayaAccount.cookie_str: cookie_str,
            XimalayaAccount.is_vip: is_vip,
        },
        synchronize_session=False,
    )
```

**关键设计：**
- 只更新 `injected=True` 的账号，不影响用户自行扫码登录的账号（`injected=False`）
- 通过 `backend_src_id` 精确匹配，只更新来自同一供体的副本
- 同时更新 `is_vip` 状态，保持一致性
- 返回 `cascade_count` 告知调用方刷新了多少个副本

#### 3.2 验证函数

**单个验证：**

```python
def verify_backend_account(backend_id: int) -> dict:
    """验证单个供体账号的 cookie 有效性"""
    # 1. 查询供体账号
    # 2. 解析 cookie_str 为 dict
    # 3. 调用 login_module.verify_login(cookies) 验证
    # 4. 更新 is_valid 和 last_verified_at
    # 5. 若有效，顺便刷新 VIP 状态和昵称
    # 6. 返回验证结果
```

**批量验证：**

```python
def verify_all_backend_accounts() -> dict:
    """批量验证供体池全部账号"""
    # 1. 查询所有供体账号 ID
    # 2. 逐个调用 verify_backend_account
    # 3. 统计有效/失效数量
    # 4. 返回汇总结果
```

**验证策略：**
- 逐个验证，一个失败不影响其余
- 验证失败时记录错误信息，便于排查
- 验证成功时顺便刷新 VIP 状态和昵称（减少额外请求）

### 4. API 接口变更 (`api/admin.py`)

新增两个接口：

#### 4.1 单个验证

```python
@router.post("/xm-login/accounts/{account_id}/verify")
async def verify_backend_xm_account(account_id: int, _: bool = Depends(get_current_admin)):
    """验证单个供体账号的 cookie 有效性"""
    result = await asyncio.to_thread(_am.verify_backend_account, account_id)
    return {"success": True, **result}
```

**返回示例：**
```json
{
  "success": true,
  "id": 1,
  "is_valid": true,
  "nickname": "用户昵称",
  "is_vip": true,
  "verified_at": "2026-08-24 12:00:00"
}
```

#### 4.2 批量验证

```python
@router.post("/xm-login/accounts/verify-all")
async def verify_all_backend_xm_accounts(_: bool = Depends(get_current_admin)):
    """批量验证供体池全部账号的 cookie 有效性"""
    result = await asyncio.to_thread(_am.verify_all_backend_accounts)
    return {"success": True, **result}
```

**返回示例：**
```json
{
  "success": true,
  "total": 5,
  "valid": 3,
  "invalid": 2,
  "details": [
    {"id": 1, "is_valid": true, "nickname": "用户1"},
    {"id": 2, "is_valid": false, "error": "Cookie 已失效"},
    ...
  ]
}
```

**路由顺序注意：**
- `verify-all` 必须在 `{account_id}/verify` 之前定义
- 否则 "verify-all" 会被误解析为 account_id

### 5. 前端变更 (`frontend/src/views/admin/AdminXmAccounts.vue`)

#### 5.1 表格新增列

- **状态列**：显示验证状态（有效/失效/未验证），使用彩色标签
- **最后验证列**：显示最后验证时间

#### 5.2 新增按钮

- **批量验证按钮**：一键验证所有供体账号
- **单个验证按钮**：每行操作列增加"验证"链接

#### 5.3 状态标签样式

```css
.tag.active { background: #10b981; color: #fff; }    /* 有效 - 绿色 */
.tag.pending { background: #f59e0b; color: #fff; }   /* 未验证 - 黄色 */
.tag.disabled { background: #ef4444; color: #fff; }  /* 失效 - 红色 */
```

#### 5.4 交互逻辑

```javascript
async function verifyOne(id) {
  verifyingId.value = id
  try {
    const r = await adminApi.post(`/xm-login/accounts/${id}/verify`)
    if (r.data.success) {
      if (r.data.is_valid) {
        toast.success(`${r.data.nickname} Cookie 有效 (VIP: ${r.data.is_vip ? '是' : '否'})`)
      } else {
        toast.error(`${r.data.id} ${r.data.error || 'Cookie 已失效'}`)
      }
      loadBackendAccounts()  // 刷新列表
    }
  } catch (e) { toast.error('验证失败') }
  verifyingId.value = null
}

async function verifyAll() {
  verifyingAll.value = true
  try {
    const r = await adminApi.post('/xm-login/accounts/verify-all')
    if (r.data.success) {
      toast.success(`验证完成: ${r.data.valid} 有效 / ${r.data.invalid} 失效 (共 ${r.data.total})`)
      loadBackendAccounts()
    }
  } catch (e) { toast.error('验证失败') }
  verifyingAll.value = false
}
```

---

## ✅ 功能验证清单

### 数据库迁移

- [ ] 启动应用后，`backend_xm_accounts` 表自动添加 `last_verified_at` 和 `is_valid` 字段
- [ ] 存量数据的 `is_valid` 和 `last_verified_at` 为 NULL（未验证状态）
- [ ] 字段可空，不影响现有数据

### 单个验证

- [ ] 点击"验证"按钮，调用 `POST /xm-login/accounts/{id}/verify`
- [ ] 验证成功：状态显示"有效"（绿色），`last_verified_at` 更新
- [ ] 验证失败：状态显示"失效"（红色），`last_verified_at` 更新
- [ ] 验证中按钮显示"验证中"，防止重复点击
- [ ] Toast 提示验证结果

### 批量验证

- [ ] 点击"批量验证全部"按钮，调用 `POST /xm-login/accounts/verify-all`
- [ ] 按钮显示"验证中…"并禁用，防止重复点击
- [ ] Toast 提示汇总结果（有效/失效/总数）
- [ ] 所有账号的验证状态和最后验证时间更新

### 级联刷新

- [ ] 用户扫码登录（触发 `add_backend_account`）：
  - 若供体池已有同 uid 账号，更新 cookie
  - 自动同步到所有 `injected=True` 且 `backend_src_id` 匹配的副本
  - 返回 `cascade_count` 表示刷新了多少个副本
- [ ] 用户自行扫码的账号（`injected=False`）不受影响
- [ ] 管理员后台扫码（`_handle_backend_poll`）同样触发级联刷新

### 前端显示

- [ ] 表格显示"状态"列（有效/失效/未验证）
- [ ] 表格显示"最后验证"列
- [ ] 状态标签颜色正确（绿色=有效，红色=失效，黄色=未验证）
- [ ] 描述文案更新，提及级联刷新功能

---

## 🎯 使用场景示例

### 场景1：管理员定期检查供体池健康状态

1. 管理员进入"供体账号管理"页面
2. 点击"批量验证全部"按钮
3. 等待验证完成，查看 Toast 提示
4. 筛选出失效的账号，删除或重新扫码登录

### 场景2：用户重新扫码登录，自动刷新已注入副本

1. 供体池中已有账号 A（uid=12345）
2. 账号 A 已注入到卡密 1、2、3（`injected=True`, `backend_src_id=1`）
3. 用户（卡密 1）重新扫码登录同一账号 A
4. `add_backend_account` 检测到同 uid，更新供体池 cookie
5. 级联刷新：卡密 1、2、3 下的注入副本自动更新为新 cookie
6. 用户自扫的账号（`injected=False`）不受影响

### 场景3：管理员验证单个账号

1. 管理员发现某个供体账号可能失效
2. 点击该行的"验证"按钮
3. 查看验证结果和 VIP 状态
4. 若失效，删除该账号或通知用户重新扫码

---

## ⚠️ 注意事项与风险

### 1. 性能考虑

- **批量验证**：逐个调用 `verify_login`，每个请求约 1-2 秒
  - 若供体池有 50 个账号，批量验证需 50-100 秒
  - 建议前端显示进度提示（当前实现为阻塞式，可优化为流式返回）
- **级联刷新**：单次 UPDATE 操作，性能影响可忽略

### 2. 并发安全

- `add_backend_account` 中的级联刷新使用单次 UPDATE，原子性有保证
- 验证操作无副作用，可并发执行

### 3. 数据一致性

- 级联刷新只更新 `injected=True` 的账号，不影响用户自扫账号
- 若用户自扫的账号与供体池同 uid，会触发级联刷新（更新其他卡的注入副本）
- 验证失败时不删除账号，仅标记为失效，由管理员决定是否删除

### 4. 向后兼容

- 新增字段全部可空，存量数据不受影响
- `is_valid=None` 表示"未验证"，前端正确显示
- 级联刷新逻辑在 `add_backend_account` 中，不影响现有调用方

---

## 🚀 后续优化建议

### 1. 定时自动验证

- 添加定时任务（如每天凌晨），自动验证所有供体账号
- 失效账号自动标记，管理员上班后可直接处理

### 2. 验证结果通知

- 批量验证完成后，若失效账号超过阈值，发送通知（邮件/钉钉/企业微信）
- 避免失效账号长期未被发现

### 3. 验证进度优化

- 批量验证改为异步任务，返回任务 ID
- 前端轮询任务状态，显示实时进度（如"已验证 15/50"）

### 4. 级联刷新日志

- 记录级联刷新日志（刷新了哪些卡密的哪些账号）
- 便于排查问题和审计

### 5. 验证失败自动删除

- 连续 N 次验证失败的账号自动删除（或移入回收站）
- 避免供体池积累大量失效账号

---

## 📝 修改文件清单

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `db/models.py` | 修改 | BackendXmAccount 新增 `last_verified_at` 和 `is_valid` 字段 |
| `db/init_db.py` | 修改 | 新增 `_migrate_backend_xm_columns()` 迁移函数 |
| `core/account_manager.py` | 修改 | `add_backend_account` 添加级联刷新；新增 `verify_backend_account` 和 `verify_all_backend_accounts`；`list_backend_accounts` 返回验证状态 |
| `api/admin.py` | 修改 | 新增 `POST /xm-login/accounts/{id}/verify` 和 `POST /xm-login/accounts/verify-all` 接口 |
| `frontend/src/views/admin/AdminXmAccounts.vue` | 修改 | 表格新增"状态"和"最后验证"列；新增"批量验证"按钮和"验证"操作；添加验证相关交互逻辑和样式 |

---

## 🧪 测试建议

### 单元测试

```python
def test_add_backend_account_cascade():
    """测试级联刷新逻辑"""
    # 1. 添加供体账号 A
    # 2. 注入到卡密 1、2
    # 3. 更新供体账号 A 的 cookie
    # 4. 验证卡密 1、2 下的注入副本 cookie 已更新
    # 5. 验证卡密 3 下用户自扫的同 uid 账号（injected=False）未受影响

def test_verify_backend_account():
    """测试验证逻辑"""
    # 1. 添加供体账号
    # 2. 验证（mock verify_login 返回成功）
    # 3. 验证 is_valid=True，last_verified_at 已更新
    # 4. 再次验证（mock verify_login 返回失败）
    # 5. 验证 is_valid=False，last_verified_at 已更新
```

### 集成测试

1. 启动应用，访问管理后台
2. 扫码登录一个测试账号到供体池
3. 点击"验证"按钮，检查状态和 Toast 提示
4. 点击"批量验证全部"，检查汇总结果
5. 注入该账号到测试卡密
6. 用户端重新扫码登录同一账号
7. 检查供体池 cookie 已更新
8. 检查注入副本 cookie 已同步更新

---

## 📚 相关文档

- [代码审查报告_2026-08-14.md](代码审查报告_2026-08-14.md)
- [代码审查报告_2026-08-16.md](代码审查报告_2026-08-16.md)
- [设备绑定需求方案.md](设备绑定需求方案.md)

---

**实施完成时间**: 2026-08-24  
**实施状态**: ✅ 已完成，待测试验证
