# Bug 排查报告：下载中断（租约失效）/ 其他设备正在下载此任务

排查时间：2026-09-12
范围：全局下载槽租约链路（`core/download_slot.py` + `api/extension.py` + `extension/background.js`）
**阶段一只定位、不改代码**；阶段二给出方案（见《修复方案_下载租约与多设备误报_2026-09-12.md》）；**本报告所列问题已全部修复并用仿真环境实测**（§7）。复现与验证已收进仓库：`tools/sim/`（见 §6/§7）。

---

## 0. 先回答你的两个疑问

| 你的疑问 | 结论 |
|---|---|
| 「下载一半提示租约失效」是不是 bug？ | **是 bug，而且是主因**。租约续约逻辑「一慢就判死刑」：只要心跳晚一拍、或服务器返回任何非 200（502/500/数据库抖动），插件立即停止整个下载，哪怕根本没有第二台设备来抢。 |
| 「多卡密不该冲突」对不对？ | **数据层你说得对——实测不冲突**（两张卡各自 acquire/heartbeat/release 完全隔离，见 §1 复现）。冲突感来自两处：①「其他设备正在下载」这句提示是插件**猜**的（服务器接口压根不返回任务归属），猜错就是误报；②多台电脑同时下载会挤同一个 Python 事件循环 + 同一个 SQLite 单写者 + 全局 `threading.Lock`，把「瞬时变慢/瞬时错误」放大成「租约失效」。 |

实测证据（`bug_repro/repro_http.py`，两张独立卡，无任何竞争者）：

```
CARD-A claim -> 200 claim_id=9c90adfa9ba84de4 lease=300
CARD-B claim -> 200 claim_id=bc0b46356ac34e2a lease=300
CARD-A heartbeat(租约刚过期 5s) -> 409 {'error': '下载租约已失效或不再有效，请停止本地下载并重新 claim'}
   服务器侧该任务状态: running | 下载槽持有者: {'task_id': '2fead6f56b18'（=A 自己）, 'claim_id': '9c90adfa…'（=A 自己）, 'expired': True}
CARD-B heartbeat(同一时刻) -> 200        ← 另一张卡完全没受影响
[A] /api/extension/tasks 单条任务字段: ['album_id','album_title','created_at','error',
     'failed_list','finished_at','fmt','progress','quality','source','status','task_id','tracks']
     ← 注意：没有 claim_id / 没有任何「谁在下载」的信息
```

同一时刻 `acquire(card=A, "local", 同一个 task_id)` 是**能成功**的（幂等续约），也就是说这次 409 完全没必要（`bug_repro/repro_slot.py` 已验证）。

---

## 1. 直接导致这两句提示的 bug（高优先级）

### A1 ★ 心跳只要不是 200 就判定「claim 失效」→ 立刻掐断正在下的文件
`extension/background.js:441-468`

```js
if (res.ok && res.data && res.data.success) { …return 'ok' }
await handleClaimLost(… || `心跳被拒 (${res.status})`)   // ← 502 / 503 / 500 / 429 / status 0 全走这里
```

只有 401 和「网络抛异常」被分开处理，其余一律当作「服务器明确拒绝了我的租约」。
可现实中这些都会走到这里：NAS 上 Docker 容器重启/更新（502、连接被拒）、前端 nginx 抖动、
后端抛异常（见 A3）、请求被 `withQueue` 长任务卡住后超时。
后果：`handleClaimLost` 把 `resolving/downloading` 的集全部打回 pending 并 `chrome.downloads.cancel()`，
一整本书停在半路。正确做法是：5xx/超时 → 走 `beatFails` 容忍逻辑（按剩余租约判断），只有 409 才是真失效。

### A2 ★ 租约「过期但没人抢」时拒绝续约，而不是原地续租
`core/download_slot.py:187-211` + `api/extension.py:327-332`

```python
UPDATE card_download_locks SET … WHERE card_id=:cid AND task_id=:tid
  AND lease_until IS NOT NULL AND lease_until > :now_s      # 过期 → rowcount 0 → False → 409
```
心跳只允许「租约还没过期」时续约。而 `acquire()` 明确支持「锁已过期 → 同一 task_id 幂等抢占」。
于是出现这种荒诞状态（上面实测复现）：锁行还在、`task_id`/`claim_id` 都还是这台设备的、
`local_tasks.status` 还是 `running`、`claim_id` 也还匹配 ——**唯一「安全续约」的口子（`heartbeat`）关了**，
只能走「中断 + 重新排队 + 重新 claim」。心跳节奏是 1 个 `chrome.alarms`（最小 1 分钟，Chrome 不保证准时），
只要某几拍被浏览器节流/机器睡眠/后台标签降级，就必然踩到。这是「偶尔下载一半就中断」的第一根因。

### A3 ★ 数据库异常被伪装成「租约被别的设备抢走了」
`core/download_slot.py`

| 函数 | 异常时返回 | 上游解读 |
|---|---|---|
| `heartbeat` `:205-210` | `(False, …)` | `api/extension.py:328` → **409 租约失效** → 插件停止下载 |
| `get_lock` `:248-251` | `None` | `api/extension.py:487-492` → **409 没有进行中的下载** → 取 cookie 时抛 `ClaimLostError` → 整本书停 |
| `acquire` `:173-175` | `(False, …)` | 409「当前卡密已有下载任务进行中」（其实没有任何任务在跑） |

`sqlite3` 的 `busy_timeout=30s`（`db/session.py:20`）一到，或多设备同时写库时，就是这些返回值。
**这就是「多台电脑一起用才偶发」的真正机制**：不是卡密互相冲突，是所有人共享一个 SQLite 写锁，
谁撞上 `database is locked`，谁的下载就被判「租约失效 / 其他设备在下载」。异常与业务拒绝必须分开（503 + 让客户端重试）。

### A4 ★ `handleClaimLost` 的兜底分支：把「半本」直接标成已完成
`extension/background.js:496-513`

```js
const serverPending = lastServerTasks.filter(…).map(…)   // 用的是「上次轮询」的缓存
…
} else {   // 列表里既没 pending 也没 running → 当作服务器已完成
  task.status = 'done'; task.claim_id = null; task.frozen = false
  if (!task.errorMsg) task.errorMsg = '下载中断（租约失效），任务可能已由服务器侧完成'
}
```
问题：
- `lastServerTasks` 是模块级变量，SW 每次冷启动都是 `[]`；函数内那次补拉**失败时也静默沿用旧值**
  （`catch {}` 无 else）。列表为空 ⇒ 判定「服务器侧已终态」⇒ **本地把 43/200 的任务标成 done + 弹「已完成」**。
- 服务器列表**只含 pending/running**，所以「任务被取消 / 卡密过期被置 cancelled / 管理员删卡删了 local_tasks /
  管理员强释锁」也全落进这一支 ⇒ 用户看到「已完成」，实际剩下的集全没下。
- 这一支不调 `/complete`、不释锁，服务器侧 `running` 要等 300s 租约自然过期才被清扫。
判据必须改成「本次列表拉取成功且不包含该任务」，且**绝不能**把非终态任务写成 `done`。

### A5 ★「其他设备正在下载此任务」是猜的，服务器没给判据
`api/extension.py:73-90`（`_task_to_dict` 无 `claim_id`/设备标识）+ `extension/background.js:814`

```js
const frozen = t.status === 'running' && t.task_id !== claimedId   // 不是我的 ⇒ 一定是「别的设备」
```
`claimedId` 只来自内存里的 `claimState`。因此以下**单机、单卡、无其他设备**的场景都会误报「其他设备正在下载」：
1. A1/A3 造成的一次心跳失败（`beatFails`/5xx/DB 异常）→ `handleClaimLost` → 服务器仍 `running` → 直接冻结 + 该提示；
2. SW 冷启动时 `loadClaimState()`（异步）尚未完成，`handleAlarm` 的 `pollBackend` 已经跑起来（`:696-711` 注册监听、`:703` 启动链、`:709` 建 alarm，三者无先后保证）⇒ `claimState=null`；
3. `claimTask` 已提交但 `saveClaimState()` 还没落盘就被杀进程；
4. 心跳 401（被顶号/会话过期）→ `clearClaimState()` 返回 `'gone'`，不做任何同步，本地任务仍 `running`，重登后即被判为「其他设备」。

被冻结后：`retryFailed` 抛「此任务正由其他设备下载，请在该设备上重试」(`:1404`)、`startTask` 直接拒绝（`:1470`）、
`pause/resume` 全部屏蔽 ——**本机自己的书本机点不动**，只能等 300s 租约过期 + 30s 清扫把它打回 pending 才解锁。
修法方向：`/api/extension/tasks` 返回 `claim_id`（或 claim 指纹/设备名/心跳时间），插件比对后区分
「我的（该恢复）」和「别人的（该冻结）」。

### A6 连续 3 次心跳不通就自我了断，不看剩余租约
`extension/background.js:331-335`、`:443-452`：`SERVER_BEAT_INTERVAL_MS=30s`、`BEAT_COMM_FAIL_LIMIT=3`
⇒ 约 90s 网络抖动即放弃，而此时租约（`LOCAL_TTL_SECONDS=300`）明明还剩 200s，锁也没人抢。
`pump()`（`:1073`）和 `pollBackend`（`:877`）两个入口都会调 `maybeHeartbeat`，**没有互斥**：
两路并发心跳里一路失败一路成功时，`beatFails` 仍被累加（成功只在各自分支清零），比设计值更容易凑满 3 次。

### A7 ★ 中断时把「正在下的集」误标成失败，续传后**永久漏集**
`extension/background.js:1266-1276` + `:406`

```js
const wasCancelledByUser = task.status === 'cancelled' || (tr.status === 'error' && tr.error === '已取消')
else if (delta.state.current === 'interrupted' && !wasCancelledByUser) { patch.status = 'error'; … }
```
`handleClaimLost` 在 `cancel()` **之前**已把这些集置回 `pending`（`:486-492`），于是 `wasCancelledByUser` 判定为 false
⇒ 取消产生的 `interrupted / USER_CANCELED` 被当成真失败，集状态变 `error`。
（注意 `USER_CANCELED` 的豁免只写在 `:1274` 那条 `delta.error` 分支里，管不到 `:1270` 的 `state` 分支——豁免写错位置。）
重新 claim 时 `:406` 的合并逻辑「保留非 pending 的历史状态」会把它们继续当 `error`
⇒ `finalizeTask` 认为「全终态 = 完成」⇒ 书标 done、`failed_list` 里躺着一堆 `USER_CANCELLED`，
不自动重试，**服务器上这本书就是缺集**。这条最能解释「中断之后回来发现少了几集」。

---

## 2. 「多台电脑」为什么会放大问题（共享资源，不是卡密冲突）

### B1 `async def` 端点里跑同步 SQLAlchemy + `threading.Lock` → 阻塞整个事件循环
`api/extension.py:42-45`（`_TASK_CREATE_LOCK`/`_CLAIM_LOCK`）、`:113`、`:282`、`:297`；
`api/extension.py` 全部生命周期端点都是 `async def` 但内部是同步 DB 调用。
`_CLAIM_LOCK` 是在**事件循环线程上**拿的进程内锁：某台设备的 claim 里那次 `acquire` 若撞上
SQLite 写锁（`busy_timeout=30`），**整台服务器的所有请求一起停摆**，其他卡密的心跳/claim 全部延迟或失败
→ 正好喂给 A1/A3。`create_local_task` 里的「先查后插」去重（含 20 行 JSON 比对）也在这个锁内。

### B2 维护循环不分卡密，全表扫 + 全表删，且同样跑在事件循环上
`app.py:99-134` 每 30s：`heartbeat(所有服务器任务)` → `sweep_expired_local_tasks()`（`core/download_slot.py:353-401`
是「先 `query(LocalTask).filter(status='running')` 拿全部卡的行，再逐行 UPDATE」）→ `expire_stale()`（全表 DELETE 过期行）。
卡多的时候这一段本身就是写放大源 + 事件循环卡顿源。

### B3 心跳节奏与服务器 TTL 完全解耦，配置一收紧就必现中断
`api/extension.py:267-268` 下发 `lease_seconds`/`heartbeat_seconds`，但 `background.js` 只用了一个写死的
30s 节流 + 1 分钟 alarm；`leaseSeconds` 存下来后**从未参与决策**。
`core/download_slot.py:34` 又允许 `max(30, DOWNLOAD_SLOT_LOCAL_TTL)`——把 TTL 配到 ≤60s（有人为了「快点重排」会这么调）
就**必然**每次下载一半就租约失效，与设备数无关。插件端应当按 `lease_seconds/3` 自适应心跳并保证「alarm 周期 < 租约」。

---

## 3. 租约/槽释放的其它缺口

### C1 「打回 pending」时不释放下载槽
- `api/skills.py:654-686` `reset_stuck_local_task`：只 `status=pending; claim_id=None`，**不调 `download_slot.release`**
  ⇒ 槽继续被那个已死任务占着，直到租约自然过期；期间同一卡密点「开始下一本」= 409「当前卡密已有下载任务进行中」，
  最长 300s（我在 `repro_slot.py` [4] 里复现了「任务 pending + 槽 alive」的组合）。
- `core/download_slot.py:353-401` `sweep_expired_local_tasks` 同样只回退任务不释槽；
  目前靠紧随其后的 `expire_stale()` 兜住，**但两者不在一个事务里**，中间那一次 `get_lock()` 会报「被占用」（`api/skills.py:508`、`api/download.py:906` 的错误提示就来自这里）。

### C2 `complete` / `cancel` / `ack` 忽略 `release()` 返回值
`api/extension.py:393`、`:433`、`:469`。释锁失败只 `logger.exception`，调用方无感；
`completeTask` 已经把 `local_tasks` 标成 done 了，槽却可能还挂着（下次 claim 别的书 409，等 5 分钟）。
至少应重试一次或返回 `slot_released:false` 让插件提示。

### C3 双份租约账本，非原子
`card_download_locks.lease_until`（`download_slot.heartbeat` 写）与 `local_tasks.lease_until`
（`api/extension.py:334-341` **另开一个 session、另一次 commit**）是两个时钟。心跳里第一次事务成功、
第二次失败（或进程此刻被杀）⇒ 任务侧租约先到期 ⇒ `sweep` 把**还在正常下载**的任务打回 pending、
下一次心跳 409。要么同一事务写两处，要么让 `sweep` 以槽行/`claim_id` 为唯一判据。

### C4 `_p()` 解析失败 = 「已过期」
`core/download_slot.py:56-65`：日期字符串格式一旦不符合 `%Y-%m-%d %H:%M:%S.%f`（手工修库、备份恢复、
换 DB），`_p` 返回 `None` → `acquire` 里 `expired = cur_lu is None or …` **判定为过期 ⇒ 任何人都能抢走锁**，
表现恰好是「两台设备同时下同一本 / 互相把对方的锁抢掉」。这种「解析失败」应当 fail-closed（视为有效并告警）。

---

## 4. 顺带发现（与本次症状不直接相关，但确认是 bug）

| # | 位置 | 问题 |
|---|---|---|
| D1 | `api/skills.py:557`（注释称「插件心跳写入 progress」）vs `api/extension.py:334-343` | 心跳端点**根本不写 progress**，progress 只在 `complete` 写 ⇒ SKILL 的 `check_local_tasks`/`check_task_status` 对本地任务永远显示 0%（`completed=0`）直到下完。插件也没有任何上报进度的通道。 |
| D2 | `api/extension.py:107` | `tracks[:_MAX_TRACKS_PER_TASK]`（5000）**静默截断**，长专辑后面的集直接消失，无提示无报错。 |
| D3 | `api/download.py:116/139/483/752`、`api/interfaces.py:501` | 服务器任务 ID 用 `str(uuid4())[:8]`（32bit）且 `_batch_tasks/_intf_tasks/_episode_jobs` 是**跨卡密的全局字典**：撞车时后一个任务直接覆盖前一个（另一张卡的任务从内存里消失、SSE 显示串号、`release` 落到别的任务上）。至少该带上 card_id 命名空间。 |
| D4 | `api/skills.py:696-699` | `card.expire_time` 字段不存在（`db/models.py:44-46` 是 `expires_at/valid_days/activated_at`）⇒ `get_card_info` 每次走 `except` 返回 `error:"'Card' object has no attribute 'expire_time'"`，该 SKILL 接口整体不可用。 |
| D5 | `core/download_slot.py:422-447` | `server_task_heartbeat_loop` 无人调用（`app.py` 用的是 `_slot_maintenance_loop`），是死代码；且它 `heartbeat()` 不传 `ttl_seconds` ⇒ 若真被用于 `local` 持有者，会把 300s 租约悄悄缩成 `SERVER_TTL`(120s)，直接制造 A2。 |
| D6 | `extension/popup.js:390-396` + `background.js:1356-1372` | 对 frozen（显示「其他设备下载中」）的任务，popup 提供「取消本地展示」：只删本地索引、不动服务器；服务器仍 `running` ⇒ 下一轮 `pollBackend` 又把它当新任务加回来，「取消」点了没反应（若 A5 是误报，就永远取消不掉）。 |
| D7 | `api/extension.py:146-161` | done 任务 30 分钟内去重返回**旧 task_id**：插件侧本地记录已被用户「清空已完成」删掉时，网页提示「已推送」，但插件队列里其实什么都没有 ⇒ 用户以为丢了任务。 |
| D8 | `extension/background.js:1195-1210`、`:65` | `TRACK_TIMEOUT=30min` 只覆盖「等待下载终态」，`resolveForTrack`（解析直链、等 cookie、等 offscreen 签名）在 `sleep(downloadDelayMs)` 之外没有任何超时；解析挂死 ⇒ 任务永远 `resolving`、`inflight` 不释放、槽被长期占住（这正是 D1/卡死类工单的来源之一）。 |

---

## 5. 修的时候建议的优先级

1. **A1 + A2 + A3**（心跳判定）：`heartbeat` 允许「同 `task_id` + 同 `claim_id` 的过期租约原地续租」；
   把 DB 异常与业务拒绝分开（异常 → 503，插件按 `beatFails` 容忍）；插件侧只有 409 才 `handleClaimLost`。
2. **A5**：`/api/extension/tasks` 补 `claim_id`（+ 心跳时间/持有者标签），插件区分「我的任务」与「别人的任务」。
3. **A4 + A7**：`handleClaimLost` 不得用陈旧/空列表下结论、不得把非终态写 `done`；取消自己发起的下载要豁免 `USER_CANCELED`。
4. **B1/B2**：把同步 DB 操作挪出事件循环（`def` 端点或 `run_in_threadpool`），`_CLAIM_LOCK` 换成 asyncio 锁 + 缩短临界区；维护循环按 card 分批。
5. **B3**：心跳间隔跟随 `lease_seconds`；服务端把 `DOWNLOAD_SLOT_LOCAL_TTL` 下限抬到 ≥ 3× 心跳上限（例如 180s）。
6. C1/C2/C3、D1、D4 顺手修。

## 6. 复现材料

阶段一用的是仓库外的两个一次性脚本（`repro_slot.py` / `repro_http.py`，直接打 `core/download_slot` 与
`TestClient`）。它们已被 **`tools/sim/`** 取代——同一批语义用真服务 + 真 HTTP + 真 SQLite 复现，并且能
"旧行为必红、新行为必绿"地跑断言：

| 本报告条目 | 现在的复现处 |
| --- | --- |
| A2 / C4（过期即被拒续约、脏 lease 被当已过期） | `tools/sim/harness.py S4`（拨 `lease_until` 到刚过期，`slot_renew_relaxed` 0/1 对照）+ `tests/test_download_slot_renew.py` |
| C1（打回 pending 不释放槽）、C3（双账本非原子） | `tests/test_extension_claim_owner.py`、`tests/test_download_slot_e2e.py` |
| B1/B2（插件把基础设施异常当所有权丢失） | `S2`（502 窗口）/ `S3`（进程 kill+重启），每条都跑 `0.7.1` 与 `0.7.2` 两套策略对照 |
| B3（多卡密其实不冲突，提示是插件猜的） | `S6`（6 卡密并发，零误报）+ `S5`（真接管才该提示，且同集双写必须为 0） |

---

## 7. 修复状态（2026-09-12 收口）

| 条目 | 状态 | 落点 |
| --- | --- | --- |
| A2 续约判定过严 | ✅ 已修 | `renew(require_live=False)` + `slot_renew_relaxed`（关=逐字节回到 0.7.1）；实测 `S4` |
| A1/C2/C3 槽与任务同事务、释放有回执 | ✅ 已修 | `api/extension.py` 心跳/complete/cancel/ack；`slot_released` 确认位 |
| C4 脏 lease 被当"已过期" | ✅ 已修（改为 fail-closed） | `_lease_of` + `_MIN_LEASE_LEN`，脏值行不可抢、不可清扫、保留现场 |
| C1 打回 pending 不释放槽 | ✅ 已修 | `expire_stale` / `sweep_expired_local_tasks` 一并清 `claim_session` |
| B1 一次 502 就判死刑 | ✅ 已修（插件 0.7.2） | `classifyBeat()` 五分类：只有服务器明确 409/404 才停；实测 `S2` |
| B2 不可达时用陈旧列表推断"服务器已完成" | ✅ 已修 | `handleClaimLost` 的 `listFresh` 闸门：**列表拉不到就绝不 finalize**。这条在修复过程中被 `S3` 升级为数据完整性事故（19/50 被标 done） |
| B3 多卡密互相冲突 | ✅ 结论维持"数据层不冲突"，并把归属暴露给插件 | `/tasks` 出 `mine`/`owner{ip,label,lease_left_seconds}`；实测 `S5`/`S6` |
| D 类顺带发现 | 🔶 部分 | `S7/S8/S9` 查出并修了 N1–N4（截断不可见 / 换格式被幂等吞 / 30min 窗口锚点错 / 每集 fmt 继承）；其余仍列在方案 §9 的 PR-5/PR-6 |
| **D4** `get_card_info` 字段名不存在 | ✅ 已修（2026-09-13） | 旧实现不是"返回 error 文案"而是**直接 500**（该函数没有 `except`，只有 `finally`）⇒ SKILL 侧完全拿不到卡密状态。改为复用 `card_public_info()` + `is_expired()`，并补 `remaining_seconds/remaining_text`；`tests/test_skill_reset_stuck_task.py` 3 例 |
| **D8** 解析阶段无超时 ⇒ 槽被无限期占住 | ✅ 已修（0.7.2） | `RESOLVE_TIMEOUT_MS=90s` + `withTimeout()` 包住 `resolveForTrack`；超时走既有 `maxRetry`，用尽后该集记 `error`、任务照常收尾并释放槽。`tests/test_plugin_beat_classify.py` 2 例（node 真跑 `withTimeout`：值透传 / 挂起必拒 / 原错误不被吞，保证 `ClaimLostError` 仍能冒泡） |
| **D1** 心跳带 progress | ⏸ 不做 | 只让 SKILL 看到本地任务百分比，代价是每拍一次 DB 写，与 PR-3 的减负方向相反 |
| **D5** `server_task_heartbeat_loop` | ⏸ 保留（死代码） | `grep` 确认无调用方 ⇒ 不可达。真被接上时必须同步 `lease_ttl ≥ 4×心跳` 这条不变式，否则就是 A2 的翻版 |

未做（有意不做，判据见方案 §13）：F5 线程池化（PR-3 —— 实测维护循环 0.5–0.8ms、最重的推送去重 5000 集也只有 64.9ms 中位，且 PR-1/2.6 已让插件对 502 免疫）、`preempt` 手动接管（PR-6 —— 后台"强制释放下载槽"已覆盖，且已回测"强释不被心跳复活"；要收敛就下调 `DOWNLOAD_SLOT_LOCAL_TTL` 到 120–180s）、`heartbeat_infra_error_mode` 切 `http_503`（PR-4 已发版，可切但收益只有日志噪音）。

> **重新评估 PR-3 的触发条件**：生产日志出现 `database is locked`；心跳 502 计数上升；`GET /tasks/list` p95 明显变大。
